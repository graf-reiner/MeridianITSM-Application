---
phase: 05-agent-mobile-and-integrations
plan: 09
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/src/app/dashboard/settings/agents/page.tsx
  - apps/web/src/app/dashboard/settings/api-keys/page.tsx
  - apps/web/src/app/dashboard/settings/webhooks/page.tsx
  - apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx
  - apps/web/src/app/dashboard/settings/alerts/page.tsx
  - apps/web/src/app/dashboard/settings/page.tsx
autonomous: true
requirements:
  - AGNT-03
  - AGNT-08
  - INTG-01
  - INTG-03
  - INTG-04
  - INTG-05
  - INTG-06

must_haves:
  truths:
    - "Agents page shows enrolled agents with status badges and enrollment token generation"
    - "API Keys page allows creation with scopes and one-time key display"
    - "Webhooks page shows webhook list with enable/disable and test delivery"
    - "Webhook detail page shows delivery history timeline"
    - "Alert Channels page shows configured channels with test delivery"
    - "All destructive actions use inline confirmation dialogs"
  artifacts:
    - path: "apps/web/src/app/dashboard/settings/agents/page.tsx"
      provides: "Agent management UI with enrollment tokens"
      contains: "Agents"
    - path: "apps/web/src/app/dashboard/settings/api-keys/page.tsx"
      provides: "API key management with one-time key display"
      contains: "API Keys"
    - path: "apps/web/src/app/dashboard/settings/webhooks/page.tsx"
      provides: "Webhook list with test delivery"
      contains: "Webhooks"
    - path: "apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx"
      provides: "Webhook detail with delivery history"
      contains: "deliveries"
    - path: "apps/web/src/app/dashboard/settings/alerts/page.tsx"
      provides: "Alert channel management"
      contains: "Alert Channels"
  key_links:
    - from: "apps/web/src/app/dashboard/settings/agents/page.tsx"
      to: "/api/v1/settings/agents"
      via: "fetch for agent list and enrollment tokens"
      pattern: "fetch.*settings/agents"
    - from: "apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx"
      to: "/api/v1/webhooks/:id API"
      via: "fetch for delivery history"
      pattern: "fetch.*webhooks"
---

<objective>
Build the web app settings pages for agent management (enrollment tokens, agent list), API key management (create with one-time display, list, revoke), webhook management (CRUD, test delivery, delivery history), and alert channel configuration.

Purpose: These settings pages are the admin control plane for Phase 5 features — agents, integrations, and alert routing.
Output: 5 new settings pages following existing dashboard inline-style patterns.
</objective>

<execution_context>
@C:/Users/greiner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/greiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-agent-mobile-and-integrations/05-CONTEXT.md
@.planning/phases/05-agent-mobile-and-integrations/05-RESEARCH.md
@.planning/phases/05-agent-mobile-and-integrations/05-UI-SPEC.md
@.planning/phases/05-agent-mobile-and-integrations/05-01-SUMMARY.md

<interfaces>
From apps/web/src/app/dashboard/settings/ (existing patterns):
- Inline styles (no Tailwind, no shadcn) — all styles via style={{ ... }}
- Page heading: 22px, fontWeight: 600, color: '#111827'
- Cards: background white, border: '1px solid #e5e7eb', borderRadius: 8
- Tables: thead background: '#f3f4f6', th/td padding: '8px 16px'
- CTA buttons: background: '#4f46e5', color: white, border: none, borderRadius: 6
- Destructive buttons: background: '#dc2626', color: white
- fetch() calls to API with auth headers from session cookie

Existing settings sidebar links pattern (from settings/page.tsx):
- Grid of setting cards linking to sub-pages

From Plan 01 — Admin agent management API routes (apps/api/src/routes/v1/settings/agents.ts):
```typescript
// These routes are created by Plan 01 Task 2 and are available for this plan to consume:
GET  /api/v1/settings/agents          — List agents for tenant (hostname, platform, status, lastHeartbeatAt, agentVersion)
GET  /api/v1/settings/agents/tokens   — List enrollment tokens for tenant
POST /api/v1/settings/agents/tokens   — Generate enrollment token (returns raw token once)
DELETE /api/v1/settings/agents/tokens/:id — Revoke enrollment token (sets isActive=false)
DELETE /api/v1/settings/agents/:id    — Delete agent record
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agents page + API Keys page</name>
  <files>
    apps/web/src/app/dashboard/settings/agents/page.tsx,
    apps/web/src/app/dashboard/settings/api-keys/page.tsx,
    apps/web/src/app/dashboard/settings/page.tsx
  </files>
  <read_first>
    apps/web/src/app/dashboard/settings/page.tsx,
    apps/web/src/app/dashboard/settings/users/page.tsx,
    apps/web/src/app/dashboard/assets/page.tsx,
    .planning/phases/05-agent-mobile-and-integrations/05-UI-SPEC.md
  </read_first>
  <action>
1. **Agents page** — `apps/web/src/app/dashboard/settings/agents/page.tsx`:

   Page structure (follows existing settings page patterns):
   - Heading: "Agents" with `<Icon path={mdiDesktopClassic} size={1} color="#4f46e5" />` (22px, fontWeight 600, color '#111827')
   - Description: "Manage enrolled inventory agents and enrollment tokens." (14px, color '#6b7280')
   - Primary CTA: "Generate Enrollment Token" button (background '#4f46e5', color white)

   **Enrollment token generation modal** (inline — not a separate page):
   - Optional expiry date picker, optional max-enrollment count (number input, default blank = unlimited)
   - On submit: POST `/api/v1/settings/agents/tokens` (created by Plan 01 — admin agent management routes)
   - Display generated token in read-only input with copy button — one-time display like API key
   - Show QR code below token (generate QR data URL containing `{ serverUrl, token }` using a simple QR library like `qrcode` npm package — install it if needed)

   **Enrollment tokens table** (collapsible section):
   - Columns: Token prefix (first 8 chars), enrollments used / max, created date, expires, status, actions
   - Status badge: Active (green) / Expired (gray) / Revoked (red)
   - Action: Revoke button → inline confirmation: "Revoke token? This token will immediately stop accepting new enrollments. Existing enrolled agents are not affected."
   - Data: GET `/api/v1/settings/agents/tokens`, DELETE `/api/v1/settings/agents/tokens/:id`

   **Agents table** (main content):
   - Columns: Hostname (clickable link, color '#4f46e5'), Platform, Status, Last Heartbeat, Agent Version, Actions
   - Status badges per UI-SPEC:
     - ONLINE: background '#d1fae5', text '#065f46'
     - OFFLINE: background '#f3f4f6', text '#6b7280'
     - STALE: background '#fef3c7', text '#92400e' (with tooltip: "Not seen in 24h")
   - Badge: inline pill, borderRadius '9999px', padding '2px 8px', fontSize '12px', fontWeight 500
   - Action: Delete button → inline confirmation: "Remove agent? This removes the agent record and its inventory history."
   - Empty state: "No agents enrolled" heading + "Generate an enrollment token..." body (per UI-SPEC)
   - Data: GET `/api/v1/settings/agents`, DELETE `/api/v1/settings/agents/:id`

2. **API Keys page** — `apps/web/src/app/dashboard/settings/api-keys/page.tsx`:

   - Heading: "API Keys" with `<Icon path={mdiKeyVariant} size={1} color="#4f46e5" />`
   - Description: "Create API keys for external integrations. Keys are shown once on creation." (per UI-SPEC)
   - Primary CTA: "Create API Key" button

   **Create modal** (inline modal with backdrop overlay):
   - Fields: Name (required text input), Scopes (multi-checkbox: tickets.read, tickets.write, assets.read, ci.read), Rate limit override (optional number, default 100/min)
   - On submit: POST `/api/v1/settings/api-keys`
   - Response: show full key in a highlighted read-only input (`background: '#fef3c7', border: '2px solid #f59e0b'`) with Copy button
   - Banner at top of list after creation: "API key created. Copy it now — it will not be shown again." (background '#fef3c7', color '#92400e')

   **Keys table**:
   - Columns: Name, Prefix (monospace), Scopes (comma-joined badges), Rate Limit, Last Used, Created, Actions
   - Action: Revoke button → inline confirmation: "Revoke key ending in ...{prefix}? Any integrations using this key will stop working immediately."
   - Empty state: "No API keys" heading + body per UI-SPEC

   Data fetching: GET `/api/v1/settings/api-keys` (from Plan 01).

3. **Update settings index** — Add navigation cards for Agents, API Keys, Webhooks, Alert Channels to `apps/web/src/app/dashboard/settings/page.tsx`.
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter web tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/dashboard/settings/agents/page.tsx contains `Agents` heading
    - apps/web/src/app/dashboard/settings/agents/page.tsx contains `Generate Enrollment Token`
    - apps/web/src/app/dashboard/settings/agents/page.tsx contains `settings/agents/tokens` API call
    - apps/web/src/app/dashboard/settings/agents/page.tsx contains `settings/agents` API call for agent list
    - apps/web/src/app/dashboard/settings/agents/page.tsx contains `#d1fae5` (ONLINE badge color)
    - apps/web/src/app/dashboard/settings/agents/page.tsx contains `#fef3c7` (STALE badge color)
    - apps/web/src/app/dashboard/settings/agents/page.tsx contains `No agents enrolled`
    - apps/web/src/app/dashboard/settings/api-keys/page.tsx contains `API Keys` heading
    - apps/web/src/app/dashboard/settings/api-keys/page.tsx contains `Create API Key`
    - apps/web/src/app/dashboard/settings/api-keys/page.tsx contains `it will not be shown again`
    - apps/web/src/app/dashboard/settings/page.tsx contains link or card for `agents` and `api-keys`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Agents page with enrollment tokens and status badges, API Keys page with one-time key display and scoped creation</done>
</task>

<task type="auto">
  <name>Task 2: Webhooks pages + Alert Channels page</name>
  <files>
    apps/web/src/app/dashboard/settings/webhooks/page.tsx,
    apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx,
    apps/web/src/app/dashboard/settings/alerts/page.tsx
  </files>
  <read_first>
    apps/web/src/app/dashboard/settings/agents/page.tsx,
    apps/web/src/app/dashboard/settings/api-keys/page.tsx,
    .planning/phases/05-agent-mobile-and-integrations/05-UI-SPEC.md
  </read_first>
  <action>
1. **Webhooks list page** — `apps/web/src/app/dashboard/settings/webhooks/page.tsx`:

   - Heading: "Webhooks" with `<Icon path={mdiWebhook} size={1} color="#4f46e5" />`
   - Description: "Receive real-time event notifications when things happen in MeridianITSM." (per UI-SPEC)
   - Primary CTA: "Add Webhook" button

   **Create/Edit form** (inline modal):
   - Name (text), URL (text, required), Events (multi-checkbox from WebhookEventType values), Custom headers (optional key-value pairs)
   - Secret auto-generated on creation

   **Webhooks table**:
   - Columns: Name (clickable link to detail), URL (truncated), Events (count badge), Status (enabled/disabled toggle or badge), Deliveries (count), Actions
   - Status badge: Enabled (green '#d1fae5'/'#065f46'), Disabled (gray), Auto-disabled (red with warning icon)
   - Auto-disabled warning text per UI-SPEC: "This webhook was automatically disabled after 50 consecutive failures. Fix the endpoint, then re-enable it."
   - Actions: "Send Test" button, Edit button, Delete button
   - "Send Test" — POST `/api/v1/webhooks/:id/test`, button loading state while in flight, inline toast on result
   - Delete → inline confirmation: "Delete webhook \"{name}\"? Delivery history will also be removed."
   - Empty state per UI-SPEC

   Data: GET `/api/v1/webhooks`

2. **Webhook detail page** — `apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx`:

   - Header: webhook name, URL, enabled/disabled badge, "Send Test" button, Edit button
   - Events list: badges showing subscribed event types
   - **Delivery history timeline** (main content):
     - List of deliveries ordered by createdAt desc
     - Each entry: status badge (success/failed/pending per UI-SPEC colors), HTTP status code, response time in ms, ISO timestamp, retry count badge (if > 0)
     - Expandable payload preview: collapsible `<pre>` block with monospace 12px font, border '1px solid #e5e7eb', background '#f3f4f6'
     - Delivery status colors per UI-SPEC:
       - success (2xx): background '#d1fae5', text '#065f46'
       - failed: background '#fee2e2', text '#991b1b'
       - pending/queued: background '#dbeafe', text '#1e40af'
   - Empty delivery state: "No deliveries yet. Trigger a test delivery to verify your endpoint." (per UI-SPEC)
   - Pagination: load more button (or auto-load on scroll)

   Data: GET `/api/v1/webhooks/:id` (includes deliveries array)

3. **Alert Channels page** — `apps/web/src/app/dashboard/settings/alerts/page.tsx`:

   - Heading: "Alert Channels" with `<Icon path={mdiBellRing} size={1} color="#4f46e5" />`
   - Description: "Send alerts to Slack, Teams, or email when critical events occur." (per UI-SPEC)
   - Primary CTA: "Add Channel" button

   **Channel card grid** (primary focal point per UI-SPEC):
   - Cards arranged in a responsive grid (3 cols on lg, 2 on md, 1 on mobile)
   - Each card: channel type icon (mdiSlack/mdiMicrosoftTeams/mdiEmailOutline), channel name, event count, enabled/disabled badge
   - Card actions: Edit, Test, Delete
   - Test: POST `/api/v1/settings/alerts/:id/test`, show result inline
   - Delete → inline confirmation: "Remove {channel name}? Alerts will no longer be sent to this channel."

   **Create/Edit modal**:
   - Channel type picker: Email / Slack / Teams (with icons)
   - Name (text)
   - Config fields (dynamic based on type):
     - Email: recipients (comma-separated emails)
     - Slack: webhook URL input (validates starts with https://hooks.slack.com/)
     - Teams: connector URL input (validates starts with https://)
   - Events multi-checkbox: select which events trigger this channel

   - Empty state per UI-SPEC: "No alert channels" + "Connect Slack or Microsoft Teams..."

   Data: GET/POST/PATCH/DELETE `/api/v1/settings/alerts`
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter web tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/dashboard/settings/webhooks/page.tsx contains `Webhooks` heading
    - apps/web/src/app/dashboard/settings/webhooks/page.tsx contains `Add Webhook`
    - apps/web/src/app/dashboard/settings/webhooks/page.tsx contains `Send Test`
    - apps/web/src/app/dashboard/settings/webhooks/page.tsx contains `50 consecutive failures` warning text
    - apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx contains `deliveries` or `delivery`
    - apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx contains `#d1fae5` (success delivery color)
    - apps/web/src/app/dashboard/settings/webhooks/[id]/page.tsx contains `#fee2e2` (failed delivery color)
    - apps/web/src/app/dashboard/settings/alerts/page.tsx contains `Alert Channels` heading
    - apps/web/src/app/dashboard/settings/alerts/page.tsx contains `Add Channel`
    - apps/web/src/app/dashboard/settings/alerts/page.tsx contains `hooks.slack.com` validation
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Webhooks list and detail pages with delivery history timeline, alert channels page with card grid and test delivery</done>
</task>

</tasks>

<verification>
- `pnpm --filter web tsc --noEmit` passes
- All 5 settings pages exist
- UI matches existing inline-style patterns
- All UI-SPEC copy strings used correctly
- Agents page fetches from /api/v1/settings/agents (admin routes from Plan 01)
</verification>

<success_criteria>
- Agents page shows status badges (ONLINE/OFFLINE/STALE), enrollment token generation with QR, agent deletion
- API Keys page creates with one-time display, lists without key, revokes
- Webhooks page CRUD with test delivery, auto-disable warning for 50 failures
- Webhook detail shows delivery history with status badges and expandable payloads
- Alert channels supports email/Slack/Teams with test delivery
- All destructive actions use inline confirmation dialogs
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-mobile-and-integrations/05-09-SUMMARY.md`
</output>
