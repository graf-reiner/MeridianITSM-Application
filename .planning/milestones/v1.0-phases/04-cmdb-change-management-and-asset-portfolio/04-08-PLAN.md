---
phase: 04-cmdb-change-management-and-asset-portfolio
plan: 08
type: execute
wave: 3
depends_on: [04-05, 04-06]
files_modified:
  - apps/web/src/app/dashboard/cmdb/import/page.tsx
  - apps/web/src/app/dashboard/applications/page.tsx
  - apps/web/src/app/dashboard/applications/[id]/page.tsx
autonomous: true
requirements: [CMDB-10, APP-05, APP-06]

must_haves:
  truths:
    - "CMDB import wizard has 3 steps: upload, column mapping, preview with validation, confirm"
    - "Application portfolio dashboard shows stat cards, interactive dependency graph, and criticality matrix"
    - "Application dependency diagram uses ReactFlow with app-specific node styling"
  artifacts:
    - path: "apps/web/src/app/dashboard/cmdb/import/page.tsx"
      provides: "3-step CMDB bulk import wizard"
    - path: "apps/web/src/app/dashboard/applications/page.tsx"
      provides: "Portfolio dashboard with stats, dependency graph, and matrix table"
    - path: "apps/web/src/app/dashboard/applications/[id]/page.tsx"
      provides: "Application detail with dependencies, documents, and assets"
  key_links:
    - from: "apps/web/src/app/dashboard/cmdb/import/page.tsx"
      to: "/api/v1/cmdb/import"
      via: "POST with validated rows"
      pattern: "api/v1/cmdb/import"
    - from: "apps/web/src/app/dashboard/applications/page.tsx"
      to: "/api/v1/applications/graph"
      via: "fetch for ReactFlow dependency graph"
      pattern: "api/v1/applications/graph"
---

<objective>
Build the CMDB bulk import wizard (3-step: upload, map, preview+confirm) and the application portfolio dashboard with visual dependency diagram and detail pages.

Purpose: The import wizard enables mass CI onboarding from CSV/JSON, and the portfolio dashboard with dependency visualization is the primary tool for application landscape management.
Output: Import wizard page, portfolio dashboard with ReactFlow dependency graph, and application detail page.
</objective>

<execution_context>
@C:/Users/greiner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/greiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cmdb-change-management-and-asset-portfolio/04-CONTEXT.md
@.planning/phases/04-cmdb-change-management-and-asset-portfolio/04-RESEARCH.md

<interfaces>
<!-- APIs from Plans 05 and 06 -->
Import API: POST /api/v1/cmdb/import — body: { rows: Array<Record<string, unknown>>, columnMap?: Record<string, string> } — returns { imported, skipped, errors: Array<{ row, errors }> }

Application API:
  GET /api/v1/applications — query: type?, status?, criticality?, search?, page?, pageSize?
  GET /api/v1/applications/stats — returns { total, byStatus, byCriticality, deprecatedCount, totalAnnualCost }
  GET /api/v1/applications/graph — returns { nodes: AppNode[], edges: AppEdge[] }
  GET /api/v1/applications/:id — includes dependencies, documents, applicationAssets, activities

ReactFlow already installed in Plan 07. papaparse needs installing for CSV parsing.

CONTEXT.md locked decisions:
  Import wizard: "3-step wizard: upload (drag-drop) -> map columns -> preview first 10 rows with validation errors highlighted -> confirm"
  Portfolio dashboard: "top stat cards -> middle interactive dependency graph -> bottom criticality/status matrix table"
  App dependency diagram: "Same ReactFlow library as CMDB, different node style (app icon, criticality badge, status indicator)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CMDB bulk import wizard</name>
  <files>apps/web/src/app/dashboard/cmdb/import/page.tsx</files>
  <read_first>
    apps/web/src/app/dashboard/cmdb/page.tsx
    apps/web/src/app/dashboard/settings/sla/page.tsx
  </read_first>
  <action>
Install papaparse in web app:
  pnpm --filter web add papaparse
  pnpm --filter web add -D @types/papaparse

Create apps/web/src/app/dashboard/cmdb/import/page.tsx:
- 'use client'
- Multi-step wizard with state machine: UPLOAD -> MAP -> PREVIEW -> IMPORTING -> COMPLETE

Step 1 - Upload:
- Drag-and-drop zone (styled div with onDragOver/onDrop) + file input button
- Accept .csv and .json files
- On file select:
  - If CSV: use papaparse.parse(file, { header: true, preview: 15, worker: false }) to parse headers and first 15 rows
    IMPORTANT: worker: false per RESEARCH.md pitfall 3 (Next.js Worker scope issue)
  - If JSON: JSON.parse the file content, extract first 15 records
- Store: parsedHeaders (string[]), previewRows (first 15), fullData (all rows)
- Auto-advance to Step 2

Step 2 - Column Mapping:
- Show detected headers on the left, CI field names on the right
- Auto-map common column names (per RESEARCH.md Pattern 6):
  'name'/'ci_name'/'ci name' -> 'name'
  'type'/'ci_type' -> 'type'
  'status' -> 'status'
  'environment'/'env' -> 'environment'
  'ip'/'ip_address' -> 'attributesJson.ipAddress'
- Dropdown for each detected header to map to a CI field (or "Skip")
- CI fields available: name (required), type, status, environment, categorySlug, description, plus attributesJson sub-fields
- Validation: at least 'name' must be mapped
- "Next" button advances to Step 3

Step 3 - Preview + Validation:
- Show first 10 rows in a table with mapped column headers
- Highlight invalid cells in red (missing required 'name', invalid type enum, etc.)
- Show row-level error messages below each invalid row
- Summary: "{X} valid rows, {Y} rows with errors"
- "Import {X} valid rows" button (errors are skipped, not blocking)
- "Back" button to return to mapping

Step 4 - Importing:
- Show progress spinner
- POST /api/v1/cmdb/import with { rows: validMappedRows, columnMap }
- On response: advance to Step 5

Step 5 - Complete:
- Show result: "{imported} imported, {skipped} skipped, {errors.length} errors"
- If errors: show downloadable error report (JSON blob as download link)
- "Import More" button to restart, "View CIs" button linking to /dashboard/cmdb

Per CONTEXT.md: "Row-level error handling: each row validated independently. Bad rows flagged with specific error. Good rows imported. Summary shows success/skip/error counts with downloadable error report"
  </action>
  <verify>
    <automated>cd C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application && test -f apps/web/src/app/dashboard/cmdb/import/page.tsx && grep -c "papaparse\|Papa" apps/web/src/app/dashboard/cmdb/import/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/web/src/app/dashboard/cmdb/import/page.tsx (file exists)
    - grep -q "papaparse\|Papa" apps/web/src/app/dashboard/cmdb/import/page.tsx (CSV parsing library)
    - grep -q "worker.*false\|worker: false" apps/web/src/app/dashboard/cmdb/import/page.tsx (worker disabled per pitfall)
    - grep -q "columnMap\|column.*map" apps/web/src/app/dashboard/cmdb/import/page.tsx (column mapping step)
    - grep -q "api/v1/cmdb/import" apps/web/src/app/dashboard/cmdb/import/page.tsx (posts to import API)
    - grep -q "drag\|drop\|onDrop\|onDragOver" apps/web/src/app/dashboard/cmdb/import/page.tsx (drag-drop upload)
    - grep -q "error" apps/web/src/app/dashboard/cmdb/import/page.tsx (error reporting)
  </acceptance_criteria>
  <done>CMDB import wizard implements full 3-step flow: drag-drop upload with papaparse CSV parsing (worker: false), column mapping with auto-detection, preview with row-level validation highlighting, and import with success/error summary and downloadable error report.</done>
</task>

<task type="auto">
  <name>Task 2: Application portfolio dashboard and detail pages</name>
  <files>apps/web/src/app/dashboard/applications/page.tsx, apps/web/src/app/dashboard/applications/[id]/page.tsx</files>
  <read_first>
    apps/web/src/app/dashboard/cmdb/[id]/page.tsx
    apps/web/src/app/dashboard/reports/page.tsx
  </read_first>
  <action>
Create apps/web/src/app/dashboard/applications/page.tsx:
Per CONTEXT.md layout: "top stat cards -> middle interactive dependency graph -> bottom criticality/status matrix table"

- 'use client'
- TanStack Query: useQuery for GET /api/v1/applications/stats and GET /api/v1/applications/graph

Section 1 - Stat Cards (top):
- 4 cards in a responsive grid (1 col mobile, 2 col md, 4 col lg):
  - Total Applications (from stats.total)
  - Critical Applications (from stats.byCriticality.CRITICAL)
  - Deprecated/Decommissioned (from stats.deprecatedCount)
  - Total Annual Cost (from stats.totalAnnualCost, formatted as currency)
- Each card: icon (mdiApplicationCog or similar), number, label, subtle background color

Section 2 - Interactive Dependency Graph (middle):
- ReactFlow component (same library as CMDB, per CONTEXT.md: "consistent zoom/pan controls")
- Fetch graph data from /api/v1/applications/graph
- Build nodes: each app as a node with custom component showing:
  - App icon (mdiApplicationCog)
  - App name
  - Criticality badge (colored: red=CRITICAL, orange=HIGH, yellow=MEDIUM, green=LOW)
  - Status indicator dot (green=ACTIVE, gray=INACTIVE, blue=IN_DEVELOPMENT)
- Build edges: from dependencies, labeled with dependencyType
- Apply dagre layout (same pattern as CMDB but can use 'LR' left-to-right for dependency chains)
- Click node to navigate to /dashboard/applications/:id
- Container height: ~400px with scroll/zoom controls

Section 3 - Criticality/Status Matrix Table (bottom):
- Fetch GET /api/v1/applications?pageSize=100
- Table with columns: name, type, status badge, criticality badge, hostingModel, lifecycleStage, annualCost
- Sortable by criticality, status
- Filter row: type dropdown, status dropdown, criticality dropdown
- "New Application" button

Create apps/web/src/app/dashboard/applications/[id]/page.tsx:
- 'use client', useParams for id
- useQuery for GET /api/v1/applications/:id
- Header: app name + type badge + status badge + criticality badge
- Sections:
  1. Details card: description, hostingModel, techStack (tag chips), authMethod, dataClassification, annualCost, rpo/rto (formatted as hours), lifecycleStage, strategicRating (1-5 stars or number)
  2. Dependencies:
     - "Depends On" list: apps this app depends on (from dependencies relation)
     - "Depended On By" list: apps that depend on this app (from dependents relation)
     - Each shows: app name, dependencyType badge, link to that app
     - "Add Dependency" button
  3. Documents:
     - List of ApplicationDocument: title, documentType badge (11 types), URL (clickable link), description
     - "Add Document" button (inline form: title, type dropdown, URL, description)
  4. Assets:
     - List of linked assets with relationship type badge (RUNS_ON, HOSTED_BY, USES) and isPrimary indicator
     - "Link Asset" button
  5. Activity Trail:
     - ApplicationActivity entries in timeline format
  </action>
  <verify>
    <automated>cd C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application && test -f apps/web/src/app/dashboard/applications/page.tsx && test -f apps/web/src/app/dashboard/applications/[id]/page.tsx && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/web/src/app/dashboard/applications/page.tsx (portfolio dashboard exists)
    - test -f apps/web/src/app/dashboard/applications/[id]/page.tsx (app detail exists)
    - grep -q "@xyflow/react\|ReactFlow" apps/web/src/app/dashboard/applications/page.tsx (ReactFlow for dependency graph)
    - grep -q "api/v1/applications/stats" apps/web/src/app/dashboard/applications/page.tsx (fetches stats)
    - grep -q "api/v1/applications/graph" apps/web/src/app/dashboard/applications/page.tsx (fetches graph)
    - grep -q "dagre" apps/web/src/app/dashboard/applications/page.tsx (dagre layout)
    - grep -q "criticality\|CRITICAL" apps/web/src/app/dashboard/applications/page.tsx (criticality shown)
    - grep -q "dependencies\|Depends" apps/web/src/app/dashboard/applications/[id]/page.tsx (dependency section)
    - grep -q "documents\|Document" apps/web/src/app/dashboard/applications/[id]/page.tsx (document section)
  </acceptance_criteria>
  <done>Portfolio dashboard has stat cards (total, critical, deprecated, annual cost), interactive ReactFlow dependency graph with app-styled nodes (icon, criticality badge, status indicator), and criticality/status matrix table. App detail page shows all fields, dependency lists (outgoing and incoming), document management with 11 types, linked assets, and activity trail.</done>
</task>

</tasks>

<verification>
- All 3 page files exist
- Import wizard uses papaparse: grep "papaparse" apps/web/src/app/dashboard/cmdb/import/page.tsx
- Portfolio uses ReactFlow: grep "ReactFlow" apps/web/src/app/dashboard/applications/page.tsx
- Stats endpoint wired: grep "stats" apps/web/src/app/dashboard/applications/page.tsx
- Graph endpoint wired: grep "graph" apps/web/src/app/dashboard/applications/page.tsx
</verification>

<success_criteria>
- CMDB import wizard: 3-step (upload with drag-drop, column mapping with auto-detect, preview with row-level validation), imports via API, shows results with error report
- Portfolio dashboard: stat cards at top, ReactFlow dependency graph in middle (app nodes with criticality badge, status indicator), criticality/status matrix at bottom
- App dependency diagram uses same ReactFlow as CMDB but with different node styling (CONTEXT.md locked decision)
- App detail page shows dependencies (both directions), documents (11 types), assets, and activity trail
</success_criteria>

<output>
After completion, create `.planning/phases/04-cmdb-change-management-and-asset-portfolio/04-08-SUMMARY.md`
</output>
