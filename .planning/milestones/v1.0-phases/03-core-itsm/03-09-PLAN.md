---
phase: 03-core-itsm
plan: 09
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-04, 03-06, 03-07, 03-08]
files_modified:
  - apps/web/src/app/dashboard/tickets/page.tsx
  - apps/web/src/app/dashboard/tickets/new/page.tsx
  - apps/web/src/app/dashboard/tickets/[id]/page.tsx
  - apps/web/src/components/SlaCountdown.tsx
  - apps/web/src/app/dashboard/knowledge/page.tsx
  - apps/web/src/app/dashboard/knowledge/new/page.tsx
  - apps/web/src/app/dashboard/knowledge/[id]/page.tsx
  - apps/web/src/components/ArticleEditor.tsx
  - apps/web/src/app/dashboard/settings/page.tsx
  - apps/web/src/app/dashboard/settings/users/page.tsx
  - apps/web/src/app/dashboard/settings/roles/page.tsx
  - apps/web/src/app/dashboard/settings/queues/page.tsx
  - apps/web/src/app/dashboard/settings/categories/page.tsx
  - apps/web/src/app/dashboard/settings/sla/page.tsx
  - apps/web/src/app/dashboard/settings/email/page.tsx
  - apps/web/src/app/dashboard/reports/page.tsx
autonomous: false
requirements: [SLA-05, REPT-01, KB-01, SETT-01, SETT-02, SETT-03, SETT-04, SETT-05, SETT-06]

must_haves:
  truths:
    - "An agent can view ticket list, create tickets, and see ticket detail with SLA countdown"
    - "SLA countdown shows green -> yellow (75%) -> red (90%) -> BREACHED color bands, with PAUSED badge when ticket is PENDING"
    - "An agent can create and edit knowledge articles using TipTap rich text editor"
    - "An admin can manage users, roles, queues, categories, SLA policies, and email accounts from settings pages"
    - "The reports dashboard displays ticket volume charts using Recharts"
  artifacts:
    - path: "apps/web/src/components/SlaCountdown.tsx"
      provides: "SLA countdown timer with color bands and PAUSED badge"
    - path: "apps/web/src/components/ArticleEditor.tsx"
      provides: "TipTap rich text editor for knowledge articles"
    - path: "apps/web/src/app/dashboard/tickets/[id]/page.tsx"
      provides: "Ticket detail page with SlaCountdown component"
    - path: "apps/web/src/app/dashboard/reports/page.tsx"
      provides: "Reports dashboard with Recharts charts"
  key_links:
    - from: "apps/web/src/app/dashboard/tickets/[id]/page.tsx"
      to: "/api/v1/tickets/:id/sla-status"
      via: "TanStack Query fetches SLA status for countdown"
      pattern: "sla-status"
    - from: "apps/web/src/components/SlaCountdown.tsx"
      to: "apps/web/src/app/dashboard/tickets/[id]/page.tsx"
      via: "SlaCountdown rendered in ticket header"
      pattern: "SlaCountdown"
    - from: "apps/web/src/app/dashboard/reports/page.tsx"
      to: "/api/v1/dashboard"
      via: "TanStack Query fetches dashboard stats for charts"
      pattern: "api/v1/dashboard"
    - from: "apps/web/src/components/ArticleEditor.tsx"
      to: "@tiptap/react"
      via: "TipTap editor integration"
      pattern: "useEditor"
---

<objective>
Build the staff-facing dashboard UI pages: ticket list/detail with SLA countdown visualization, knowledge base with TipTap editor, admin settings pages, and reports dashboard with Recharts.

Purpose: Without frontend pages, the API backends built in Plans 01-08 are unreachable by users. This plan delivers the core dashboard experience that makes the ITSM product usable for agents and admins. The SLA countdown is a locked CONTEXT.md decision critical to the roadmap success criteria.
Output: 16+ dashboard pages covering tickets, knowledge, settings, and reports.
</objective>

<execution_context>
@C:/Users/greiner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/greiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-itsm/03-CONTEXT.md
@.planning/phases/03-core-itsm/03-RESEARCH.md
@.planning/phases/03-core-itsm/03-01-SUMMARY.md
@.planning/phases/03-core-itsm/03-02-SUMMARY.md
@.planning/phases/03-core-itsm/03-04-SUMMARY.md
@.planning/phases/03-core-itsm/03-06-SUMMARY.md
@.planning/phases/03-core-itsm/03-08-SUMMARY.md

<interfaces>
From CONTEXT.md (locked decisions -- MUST implement exactly):
- Countdown timer with color bands: green -> yellow (75%) -> red (90%) -> breached
- Paused SLA shows "PAUSED" badge with frozen timer and reason
- SLA breach triggers: red "BREACHED" badge on ticket

From 03-RESEARCH.md (SLA Countdown Timer Component pattern):
```typescript
'use client';
import { useState, useEffect } from 'react';

function SlaCountdown({ slaBreachAt, isPaused, elapsedPercentage }: {
  slaBreachAt: string | null;
  isPaused: boolean;
  elapsedPercentage: number;
}) {
  // setInterval 1-second updates
  // Color bands: green (< 75%), yellow (>= 75%), red (>= 90%), BREACHED (>= 100%)
  // PAUSED badge when isPaused=true
}
```

From 03-RESEARCH.md (TipTap Editor pattern):
```typescript
'use client';
import { useEditor, EditorContent } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
import Image from '@tiptap/extension-image';
import Link from '@tiptap/extension-link';
import CodeBlockLowlight from '@tiptap/extension-code-block-lowlight';
```

API endpoints (from Plans 01, 02, 04, 06, 08):
- GET /api/v1/tickets -- ticket list with filters
- POST /api/v1/tickets -- create ticket
- GET /api/v1/tickets/:id -- ticket detail
- PATCH /api/v1/tickets/:id -- update ticket
- GET /api/v1/tickets/:id/sla-status -- live SLA data
- GET /api/v1/knowledge -- article list
- POST /api/v1/knowledge -- create article
- GET /api/v1/knowledge/:id -- article detail
- PATCH /api/v1/knowledge/:id -- update article
- GET /api/v1/settings/users -- user list
- GET /api/v1/settings/roles -- role list
- GET /api/v1/settings/queues -- queue list
- GET /api/v1/settings/categories -- category list
- GET /api/v1/sla -- SLA policy list
- GET /api/v1/email-accounts -- email account list
- GET /api/v1/dashboard -- dashboard stats with chart data

Frontend patterns (from existing codebase):
- apps/web/src/hooks/usePlan.ts -- TanStack Query hook pattern
- apps/web/src/app/billing/page.tsx -- reference authenticated page
- Icons: @mdi/react + @mdi/js (Icon component with path prop)
- CSS: Tailwind CSS 4 + shadcn/ui components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ticket pages with SLA countdown and knowledge base with TipTap editor</name>
  <files>
    apps/web/src/app/dashboard/tickets/page.tsx,
    apps/web/src/app/dashboard/tickets/new/page.tsx,
    apps/web/src/app/dashboard/tickets/[id]/page.tsx,
    apps/web/src/components/SlaCountdown.tsx,
    apps/web/src/app/dashboard/knowledge/page.tsx,
    apps/web/src/app/dashboard/knowledge/new/page.tsx,
    apps/web/src/app/dashboard/knowledge/[id]/page.tsx,
    apps/web/src/components/ArticleEditor.tsx
  </files>
  <read_first>
    apps/web/src/app/billing/page.tsx,
    apps/web/src/hooks/usePlan.ts,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/portal/tickets/page.tsx,
    apps/web/src/app/portal/knowledge/page.tsx,
    apps/web/package.json
  </read_first>
  <action>
1. **Install TipTap and Recharts in apps/web** (if not already):
   ```bash
   pnpm --filter web add @tiptap/react @tiptap/starter-kit @tiptap/extension-image @tiptap/extension-link @tiptap/extension-code-block-lowlight lowlight recharts isomorphic-dompurify date-fns
   ```

2. **Create `apps/web/src/components/SlaCountdown.tsx`** (SLA-05 -- LOCKED DECISION):
   - 'use client' component
   - Props: `{ slaBreachAt: string | null; isPaused: boolean; elapsedPercentage: number; pauseReason?: string }`
   - Use `useState` + `useEffect` with `setInterval(1000)` to update remaining time every second
   - Calculate remaining: `new Date(slaBreachAt).getTime() - Date.now()`
   - Format remaining as `Xh Ym` (hours and minutes) or `Xm Ys` (if under 1 hour)
   - **Color bands per CONTEXT.md locked decision**:
     - Green (`text-green-600 bg-green-50`): elapsedPercentage < 75
     - Yellow (`text-yellow-600 bg-yellow-50`): elapsedPercentage >= 75 and < 90
     - Red (`text-red-600 bg-red-50`): elapsedPercentage >= 90 and < 100
     - BREACHED (`text-red-700 bg-red-100 font-bold`): elapsedPercentage >= 100 -- show "BREACHED" text instead of countdown
   - **PAUSED state per CONTEXT.md**: When `isPaused=true`, show badge: `<span className="bg-orange-100 text-orange-700 px-2 py-0.5 rounded text-xs font-medium">PAUSED</span>` with frozen timer (clear interval). Show `pauseReason` tooltip if provided.
   - Compact enough for ticket header area (inline-flex, small text)
   - Cleanup: clear interval on unmount

3. **Create `apps/web/src/app/dashboard/tickets/page.tsx`** (Ticket List):
   - TanStack Query: `useQuery({ queryKey: ['tickets', filters], queryFn: () => fetch('/api/v1/tickets?' + params) })`
   - Toolbar: search input, status filter (dropdown), priority filter, assignee filter, category filter
   - Table/list with columns: TKT-XXXXX, Title, Status (badge), Priority (badge), Assignee, Category, Created (relative), SLA status (mini color dot from elapsed percentage)
   - Pagination controls (page/pageSize)
   - "New Ticket" button linking to /dashboard/tickets/new
   - Icons via @mdi/react: mdiTicket, mdiPlus, mdiMagnify, mdiFilter

4. **Create `apps/web/src/app/dashboard/tickets/new/page.tsx`** (Create Ticket):
   - React Hook Form + Zod for validation
   - Fields: title (required), description (textarea), type (dropdown: INCIDENT/SERVICE_REQUEST/PROBLEM), priority (dropdown), category (dropdown from GET /api/v1/settings/categories), queue (dropdown from GET /api/v1/settings/queues), SLA policy (dropdown from GET /api/v1/sla), assigned to (dropdown from GET /api/v1/settings/users?isActive=true)
   - Submit: POST /api/v1/tickets
   - On success: redirect to /dashboard/tickets/[id]
   - Use shadcn/ui form components (Input, Select, Textarea, Button)

5. **Create `apps/web/src/app/dashboard/tickets/[id]/page.tsx`** (Ticket Detail):
   - TanStack Query: fetch ticket detail + SLA status in parallel
   - **Header area**: ticket number, title (editable inline), status badge, priority badge, **SlaCountdown component** (fetched from GET /api/v1/tickets/:id/sla-status, pass slaBreachAt, isPaused, elapsedPercentage)
   - **Sidebar**: assignee (editable dropdown), queue, category, SLA policy, created/updated timestamps, requester info
   - **Tabs**: Comments, Activity, Attachments, Linked Articles
   - **Comments tab**: list all comments (PUBLIC shown to all, INTERNAL shown with "Internal" badge only to staff). Comment form with rich textarea, visibility toggle (PUBLIC/INTERNAL), optional time tracking input.
   - **Activity tab**: paginated TicketActivity log (from GET /api/v1/tickets/:id/activities)
   - **Attachments tab**: list with download buttons (GET .../attachments/:id/url), upload button (POST .../attachments)
   - **Status change**: dropdown in header area to transition ticket status (shows only valid transitions)

6. **Create `apps/web/src/components/ArticleEditor.tsx`** (TipTap Editor):
   - 'use client' component
   - Props: `{ initialContent?: string; onChange: (html: string) => void; editable?: boolean }`
   - Use `useEditor` from `@tiptap/react` with extensions:
     - `StarterKit.configure({ codeBlock: false })`
     - `Image`
     - `Link.configure({ openOnClick: false })`
     - `CodeBlockLowlight.configure({ lowlight: createLowlight() })`
   - Toolbar: Bold, Italic, Heading (H1-H3), Bullet List, Ordered List, Code Block, Link, Image (URL input), Blockquote
   - Use shadcn/ui Button for toolbar items with active state styling
   - Render: `<EditorContent editor={editor} className="prose max-w-none min-h-[300px]" />`
   - Sanitize output with `DOMPurify.sanitize()` before calling onChange (prevent XSS per RESEARCH.md pitfall)

7. **Create `apps/web/src/app/dashboard/knowledge/page.tsx`** (Article List):
   - TanStack Query: fetch from GET /api/v1/knowledge with filters
   - Toolbar: search, status filter (DRAFT/IN_REVIEW/PUBLISHED/RETIRED), visibility filter
   - Table: Title, Status (badge), Author, View Count, Helpful Count, Updated
   - "New Article" button

8. **Create `apps/web/src/app/dashboard/knowledge/new/page.tsx`** (Create Article):
   - React Hook Form: title, summary, tags (comma-separated input), visibility (PUBLIC/INTERNAL)
   - ArticleEditor component for content field
   - Submit: POST /api/v1/knowledge
   - On success: redirect to article detail

9. **Create `apps/web/src/app/dashboard/knowledge/[id]/page.tsx`** (Article Detail/Edit):
   - If user has knowledge:write permission: show ArticleEditor in edit mode
   - Status transition buttons (DRAFT -> IN_REVIEW -> PUBLISHED -> RETIRED) based on current status
   - Sidebar: author, created/updated dates, view count, helpful count, tags, visibility
   - Read-only mode for users without write permission: render sanitized HTML content
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Ticket list, create, and detail pages are functional with SLA countdown visualization showing color bands (green/yellow/red/BREACHED) and PAUSED badge. Knowledge base pages support article CRUD with TipTap rich text editor including XSS sanitization.</done>
</task>

<task type="auto">
  <name>Task 2: Settings UI pages and reports dashboard with Recharts</name>
  <files>
    apps/web/src/app/dashboard/settings/page.tsx,
    apps/web/src/app/dashboard/settings/users/page.tsx,
    apps/web/src/app/dashboard/settings/roles/page.tsx,
    apps/web/src/app/dashboard/settings/queues/page.tsx,
    apps/web/src/app/dashboard/settings/categories/page.tsx,
    apps/web/src/app/dashboard/settings/sla/page.tsx,
    apps/web/src/app/dashboard/settings/email/page.tsx,
    apps/web/src/app/dashboard/reports/page.tsx
  </files>
  <read_first>
    apps/web/src/app/dashboard/tickets/page.tsx,
    apps/web/src/app/billing/page.tsx,
    apps/web/src/hooks/usePlan.ts,
    apps/web/src/components/SlaCountdown.tsx
  </read_first>
  <action>
1. **Create `apps/web/src/app/dashboard/settings/page.tsx`** (Settings Hub):
   - Grid of settings section cards linking to sub-pages:
     - Users (/dashboard/settings/users) -- icon: mdiAccountGroup
     - Roles (/dashboard/settings/roles) -- icon: mdiShieldAccount
     - Queues (/dashboard/settings/queues) -- icon: mdiTrayFull
     - Categories (/dashboard/settings/categories) -- icon: mdiTag
     - SLA Policies (/dashboard/settings/sla) -- icon: mdiClockAlert
     - Email Accounts (/dashboard/settings/email) -- icon: mdiEmail
   - Use shadcn/ui Card components with icon, title, and description

2. **Create `apps/web/src/app/dashboard/settings/users/page.tsx`** (SETT-01):
   - TanStack Query: GET /api/v1/settings/users with search and pagination
   - Table: Name, Email, Role, Active (badge), Created
   - Actions: Edit (opens modal/drawer), Disable/Enable toggle, Reset Password
   - Create user button: opens modal with form (firstName, lastName, email, password, role dropdown)
   - Use shadcn/ui Dialog for modals, Table for list

3. **Create `apps/web/src/app/dashboard/settings/roles/page.tsx`** (SETT-02):
   - TanStack Query: GET /api/v1/settings/roles
   - Table: Name, Type (System/Custom badge), Permission Count
   - Create custom role: form with name and permission checkboxes
   - Permission editor: grouped checkboxes for each permission category
   - System roles shown as read-only (cannot edit/delete)

4. **Create `apps/web/src/app/dashboard/settings/queues/page.tsx`** (SETT-04):
   - TanStack Query: GET /api/v1/settings/queues
   - Table: Name, Auto-Assign (toggle), Default Assignee, Ticket Count
   - Create/edit queue: form with name, autoAssign toggle, defaultAssigneeId dropdown
   - Assignment rules JSON editor (simple textarea for now, per Claude's discretion)

5. **Create `apps/web/src/app/dashboard/settings/categories/page.tsx`** (SETT-06):
   - TanStack Query: GET /api/v1/settings/categories/tree for hierarchical view
   - Render as indented tree list showing parent-child relationships
   - Create/edit category: form with name, parentId (dropdown of existing categories), icon picker, color picker, description
   - Delete: confirm dialog, blocked if children exist

6. **Create `apps/web/src/app/dashboard/settings/sla/page.tsx`** (SETT-05):
   - TanStack Query: GET /api/v1/sla
   - Table: Name, P1/P2/P3/P4 response times, Business Hours (yes/no badge), Timezone, Auto-Escalate (yes/no badge)
   - Create/edit SLA: form with all fields including:
     - Name, priority response/resolution minute inputs for P1-P4
     - Business hours toggle, start/end time inputs, business days checkboxes (Mon-Sun)
     - Timezone dropdown (list of common IANA timezones)
     - Auto-escalate toggle + escalation queue dropdown (per CONTEXT.md locked decision for "configurable auto-escalation option")

7. **Create `apps/web/src/app/dashboard/settings/email/page.tsx`** (email account management):
   - TanStack Query: GET /api/v1/email-accounts
   - Table: Name, Email Address, SMTP (configured badge), IMAP (configured badge), Active, Last Polled
   - Create/edit: form with email, SMTP config (host, port, user, password, secure), IMAP config (host, port, user, password, secure), default queue, default category
   - "Test SMTP" and "Test IMAP" buttons calling POST /api/v1/email-accounts/test-smtp and test-imap
   - Password fields: show "configured" if existing, empty if new

8. **Create `apps/web/src/app/dashboard/reports/page.tsx`** (REPT-01):
   - TanStack Query: GET /api/v1/dashboard for stats data
   - **Stats cards** (top row): Total Tickets, Open Tickets, Resolved Today, Overdue (SLA Breached) -- using shadcn/ui Card with large number and icon
   - **Charts using Recharts**:
     - Ticket volume by day (last 30 days): `<LineChart>` with date x-axis, ticket count y-axis
     - Tickets by priority: `<BarChart>` showing LOW/MEDIUM/HIGH/CRITICAL counts
     - Top categories: `<PieChart>` showing top 5 categories by ticket count
   - **Recent Activity** feed: last 10 TicketActivity records with timestamp, actor, and action description
   - **Export buttons**: "Export Tickets CSV" (links to GET /api/v1/reports/tickets?format=csv), "SLA Report" (links to /api/v1/reports/sla-compliance)
   - Import Recharts components: `import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, BarChart, Bar, PieChart, Pie, Cell } from 'recharts';`
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>All settings pages are functional: users with CRUD and password reset, roles with permission editor, queues with auto-assignment, categories as tree, SLA policies with business hours and auto-escalation config, email accounts with connection testing. Reports dashboard shows ticket stats, Recharts charts, and export buttons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of staff dashboard</name>
  <action>
  Human verifies the complete staff dashboard UI renders correctly.
  What was built: ticket management with SLA countdown (color bands: green/yellow/red/BREACHED + PAUSED badge), knowledge base with TipTap editor, settings pages, and reports dashboard with Recharts charts.
  </action>
  <what-built>Complete staff dashboard UI: ticket management with SLA countdown (color bands: green/yellow/red/BREACHED + PAUSED badge), knowledge base with TipTap editor, settings pages, and reports dashboard with Recharts charts.</what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm --filter web dev`
    2. Log in as agent (agent@msp.local / Agent123!)
    3. Navigate to /dashboard/tickets -- verify ticket list renders with filters
    4. Click "New Ticket" -- verify form with category, queue, SLA dropdowns
    5. Create a ticket with an SLA policy assigned -- go to ticket detail
    6. Verify SLA countdown appears in ticket header with color band (should be green if SLA is not close to breach)
    7. Navigate to /dashboard/knowledge -- verify article list
    8. Click "New Article" -- verify TipTap editor renders with toolbar (bold, italic, headings, links)
    9. Navigate to /dashboard/settings -- verify settings hub with cards
    10. Click into Settings > SLA Policies -- verify auto-escalation toggle and queue dropdown
    11. Navigate to /dashboard/reports -- verify Recharts charts render (line chart, bar chart, pie chart)
    12. Verify "Export CSV" button triggers download
  </how-to-verify>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>User has verified the staff dashboard renders correctly with SLA countdown, TipTap editor, settings pages, and Recharts charts.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter web exec tsc --noEmit` succeeds
- SlaCountdown component renders color bands matching CONTEXT.md specification
- TipTap ArticleEditor renders with toolbar and XSS sanitization
- All settings pages render and connect to their API endpoints
- Recharts charts render in reports dashboard
- All pages use @mdi/react icons (not webfont)
</verification>

<success_criteria>
- Ticket detail page shows SLA countdown with green/yellow/red/BREACHED color bands
- PAUSED tickets show orange PAUSED badge instead of countdown
- TipTap editor supports bold, italic, headings, code blocks, images, links
- Settings pages allow CRUD for users, roles, queues, categories, SLA policies, email accounts
- SLA policy editor includes auto-escalation toggle and queue dropdown per CONTEXT.md
- Reports dashboard displays Recharts line/bar/pie charts with real data from /api/v1/dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-itsm/03-09-SUMMARY.md`
</output>
