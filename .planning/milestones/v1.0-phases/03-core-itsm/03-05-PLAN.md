---
phase: 03-core-itsm
plan: 05
type: execute
wave: 2
depends_on: [03-01, 03-04]
files_modified:
  - apps/web/src/app/portal/layout.tsx
  - apps/web/src/app/portal/page.tsx
  - apps/web/src/app/portal/tickets/page.tsx
  - apps/web/src/app/portal/tickets/new/page.tsx
  - apps/web/src/app/portal/tickets/[id]/page.tsx
  - apps/web/src/app/portal/knowledge/page.tsx
  - apps/web/src/app/portal/assets/page.tsx
  - apps/web/src/middleware.ts
autonomous: true
requirements: [PRTL-01, PRTL-02, PRTL-03, PRTL-04, PRTL-05, PRTL-06]

must_haves:
  truths:
    - "End users can access /portal with a simplified layout and sidebar"
    - "End users can submit service requests via category-driven form"
    - "End users can view their ticket status and add PUBLIC comments"
    - "End users can browse published knowledge articles"
    - "End users can view their assigned assets"
    - "Middleware auto-redirects end_user role from /dashboard to /portal"
  artifacts:
    - path: "apps/web/src/app/portal/layout.tsx"
      provides: "Portal layout with simplified sidebar navigation"
    - path: "apps/web/src/app/portal/tickets/new/page.tsx"
      provides: "Category-driven service request form"
    - path: "apps/web/src/middleware.ts"
      provides: "end_user role redirect middleware"
  key_links:
    - from: "apps/web/src/middleware.ts"
      to: "/portal"
      via: "JWT role check redirects end_user to portal"
      pattern: "end_user.*portal"
    - from: "apps/web/src/app/portal/tickets/new/page.tsx"
      to: "/api/v1/tickets"
      via: "form submission creates ticket via API"
      pattern: "fetch.*api/v1/tickets"
---

<objective>
Build the end-user self-service portal with ticket submission, status tracking, KB browsing, asset viewing, and role-based redirect middleware.

Purpose: End users need a simplified interface to submit requests and track progress without access to staff-only features.
Output: Portal pages with simplified layout, service request form, and auto-redirect middleware.
</objective>

<execution_context>
@C:/Users/greiner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/greiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-itsm/03-CONTEXT.md
@.planning/phases/03-core-itsm/03-RESEARCH.md
@.planning/phases/03-core-itsm/03-01-SUMMARY.md
@.planning/phases/03-core-itsm/03-04-SUMMARY.md

<interfaces>
From apps/web/src/app/layout.tsx (existing pattern):
```typescript
// Next.js App Router layout with providers
```

From CONTEXT.md (locked decisions):
- Portal shares same shadcn/ui component library and theme as dashboard
- Service request form is category-driven: user picks category first
- End users see simplified SLA info: "We aim to respond within 4 hours" with green/yellow/red indicator
- Middleware auto-redirects end_user role from /dashboard to /portal
- Middleware matcher MUST exclude API routes to prevent redirect loops

API endpoints (from Plan 01 and 04):
- GET /api/v1/tickets?requestedById={userId} -- user's tickets
- POST /api/v1/tickets -- create ticket
- POST /api/v1/tickets/:id/comments -- add comment
- GET /api/v1/knowledge/published -- published articles
- GET /api/v1/tickets/:id/sla-status -- SLA countdown data
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal layout, middleware redirect, and home page</name>
  <files>
    apps/web/src/app/portal/layout.tsx,
    apps/web/src/app/portal/page.tsx,
    apps/web/src/middleware.ts
  </files>
  <read_first>
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/app/billing/page.tsx,
    apps/web/src/middleware.ts
  </read_first>
  <action>
1. **Create or update `apps/web/src/middleware.ts`**:
   - Import `NextResponse` from `next/server`, `jwtVerify` from `jose`
   - Check JWT cookie/header for `role` claim
   - If role is `end_user` AND path starts with `/dashboard`: redirect to `/portal`
   - If role is NOT `end_user` AND path starts with `/portal`: allow (staff can view portal too, but not redirected)
   - If no JWT and path requires auth: redirect to `/login`
   - Config matcher: `['/((?!api|_next/static|_next/image|favicon.ico|login|signup).*)']` -- CRITICAL: exclude /api routes to prevent redirect loops on TanStack Query fetches

2. **Create `apps/web/src/app/portal/layout.tsx`**:
   - Simplified sidebar with navigation items:
     - Home (/ portal) -- icon: mdiHome
     - My Tickets (/portal/tickets) -- icon: mdiTicketOutline
     - New Request (/portal/tickets/new) -- icon: mdiPlus
     - Knowledge Base (/portal/knowledge) -- icon: mdiBookOpenVariant
     - My Assets (/portal/assets) -- icon: mdiLaptop
   - Use same shadcn/ui components as dashboard layout (Card, Button, etc.)
   - Include notification bell with unread count (fetches GET /api/v1/notifications?unread=true&count=true)
   - User profile dropdown with logout
   - Responsive: sidebar collapses to bottom nav on mobile
   - Import icons via `@mdi/react` + `@mdi/js` (e.g., `import Icon from '@mdi/react'; import { mdiHome } from '@mdi/js';`)

3. **Create `apps/web/src/app/portal/page.tsx`** (Portal Home):
   - Greeting: "Welcome back, {user.firstName}"
   - Quick stats cards: Open Tickets (count), Pending Tickets (count)
   - Recent tickets list (last 5, with status badge and relative time)
   - Quick action buttons: "Submit New Request", "Browse Knowledge Base"
   - Fetch from: GET /api/v1/tickets?requestedById={userId}&pageSize=5
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/middleware.ts contains `end_user` role check
    - apps/web/src/middleware.ts contains `/portal` redirect target
    - apps/web/src/middleware.ts contains `(?!api` in matcher (API route exclusion)
    - apps/web/src/app/portal/layout.tsx contains `mdiHome` or `@mdi/js` (MDI icons)
    - apps/web/src/app/portal/layout.tsx contains `portal/tickets` (navigation link)
    - apps/web/src/app/portal/layout.tsx contains `portal/knowledge` (navigation link)
    - apps/web/src/app/portal/page.tsx contains `Welcome` or greeting text
    - apps/web/src/app/portal/page.tsx contains `api/v1/tickets` (data fetch)
  </acceptance_criteria>
  <done>Portal layout renders with simplified sidebar navigation and notification bell. Middleware redirects end_user role to /portal without blocking API routes. Portal home shows greeting, ticket stats, and quick actions.</done>
</task>

<task type="auto">
  <name>Task 2: Portal ticket pages, KB browsing, and assets page</name>
  <files>
    apps/web/src/app/portal/tickets/page.tsx,
    apps/web/src/app/portal/tickets/new/page.tsx,
    apps/web/src/app/portal/tickets/[id]/page.tsx,
    apps/web/src/app/portal/knowledge/page.tsx,
    apps/web/src/app/portal/assets/page.tsx
  </files>
  <read_first>
    apps/web/src/app/portal/layout.tsx,
    apps/web/src/app/portal/page.tsx,
    apps/web/src/app/billing/page.tsx,
    apps/web/src/hooks/usePlan.ts
  </read_first>
  <action>
1. **Create `apps/web/src/app/portal/tickets/page.tsx`** (My Tickets list):
   - Fetch: GET /api/v1/tickets?requestedById={userId} with pagination
   - Display as card list: ticket number (TKT-XXXXX), title, status badge, priority badge, relative time
   - Status badges: color-coded (NEW=blue, OPEN=green, IN_PROGRESS=yellow, PENDING=orange, RESOLVED=gray, CLOSED=gray, CANCELLED=red)
   - Filter by status (tabs: All, Open, Resolved, Closed)
   - Link each card to /portal/tickets/[id]

2. **Create `apps/web/src/app/portal/tickets/new/page.tsx`** (Submit Service Request):
   - **Category-driven form** per CONTEXT.md locked decision:
     - Step 1: Category picker -- fetch GET /api/v1/settings/categories, display as card grid. User selects one.
     - Step 2: After category selected, show form with: title (required), description (rich text area), priority dropdown (default MEDIUM), file upload area
     - Type auto-set to `SERVICE_REQUEST`
   - Submit: POST /api/v1/tickets with `{ title, description, type: 'SERVICE_REQUEST', priority, categoryId }`
   - On success: redirect to /portal/tickets/[newTicketId] with success toast
   - Use React Hook Form + Zod for validation

3. **Create `apps/web/src/app/portal/tickets/[id]/page.tsx`** (Ticket Detail):
   - Fetch: GET /api/v1/tickets/:id
   - Header: ticket number, title, status badge, priority badge
   - **Simplified SLA display** per CONTEXT.md: "We aim to respond within X hours" with green/yellow/red indicator
     - Fetch GET /api/v1/tickets/:id/sla-status
     - Show simple colored indicator: green (OK), yellow (WARNING), red (CRITICAL/BREACHED)
     - Do NOT show detailed countdown timer (that's staff-only per CONTEXT.md)
   - Comments section: show only PUBLIC comments (API already filters by user role context)
   - Add comment form: text area + submit button (visibility forced to PUBLIC for end users)
   - Attachments list: file names with download links (GET /api/v1/tickets/:id/attachments/:aid/url)
   - Timeline: show activities (created, status changed, commented) in chronological order

4. **Create `apps/web/src/app/portal/knowledge/page.tsx`** (Browse Knowledge):
   - Fetch: GET /api/v1/knowledge/published with search
   - Search bar at top
   - Article cards: title, summary (truncated), tags, viewCount, helpfulCount
   - Click opens article detail inline or in a modal (using shadcn/ui Dialog)
   - Article detail shows: full HTML content rendered safely, vote buttons (helpful/not helpful)
   - Vote: POST /api/v1/knowledge/:id/vote

5. **Create `apps/web/src/app/portal/assets/page.tsx`** (My Assets):
   - Fetch: GET /api/v1/assets?assignedToId={userId} (API route from Phase 4 won't exist yet)
   - For now: show empty state "No assets assigned to you" with placeholder message "Asset management will be available soon"
   - This satisfies PRTL-05 structurally -- the API endpoint will be wired when Phase 4 implements asset CRUD
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/portal/tickets/page.tsx contains `requestedById` (filters to user's own tickets)
    - apps/web/src/app/portal/tickets/new/page.tsx contains `SERVICE_REQUEST` (auto-set ticket type)
    - apps/web/src/app/portal/tickets/new/page.tsx contains `categoryId` (category-driven form)
    - apps/web/src/app/portal/tickets/[id]/page.tsx contains `sla-status` (simplified SLA display)
    - apps/web/src/app/portal/tickets/[id]/page.tsx contains `PUBLIC` (comment visibility)
    - apps/web/src/app/portal/knowledge/page.tsx contains `published` (published-only articles)
    - apps/web/src/app/portal/knowledge/page.tsx contains `vote` (helpful/not helpful)
    - apps/web/src/app/portal/assets/page.tsx exists and contains placeholder content
    - All 5 portal page files exist
  </acceptance_criteria>
  <done>Portal provides complete end-user experience: ticket list filtered to user's own, category-driven service request form, ticket detail with simplified SLA indicator and public comments, knowledge article browsing with search and voting, and assets placeholder page.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web exec tsc --noEmit` succeeds
- All 7 portal page files exist under apps/web/src/app/portal/
- Middleware redirects end_user role without blocking API routes
- Service request form uses category-driven approach per CONTEXT.md
- Portal ticket detail shows simplified SLA (not full countdown per CONTEXT.md)
- End users only see PUBLIC comments
</verification>

<success_criteria>
- End-user accessing /dashboard gets redirected to /portal
- Portal has simplified sidebar with 5 nav items
- Service request form selects category first, then shows form fields
- Ticket detail shows green/yellow/red SLA indicator, not full countdown
- Knowledge browsing shows only PUBLISHED + PUBLIC articles
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-itsm/03-05-SUMMARY.md`
</output>
