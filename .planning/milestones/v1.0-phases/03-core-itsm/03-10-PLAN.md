---
phase: 03-core-itsm
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/__tests__/tickets.test.ts
  - apps/api/src/__tests__/ticket-service.test.ts
  - apps/api/src/__tests__/email-inbound.test.ts
  - apps/api/src/__tests__/notification-service.test.ts
  - apps/api/src/__tests__/reports.test.ts
  - apps/api/src/test-utils/setup.ts
autonomous: true
requirements: [TICK-01, TICK-02, TICK-03, EMAL-03, EMAL-04, NOTF-04, REPT-02]

must_haves:
  truths:
    - "Test scaffold files exist for all core service domains before implementation begins"
    - "Each test file has describe blocks and pending/skipped tests for expected behaviors"
    - "Test utilities provide Prisma mock and Fastify test app builder"
  artifacts:
    - path: "apps/api/src/__tests__/tickets.test.ts"
      provides: "Ticket API integration test stubs for TICK-01, TICK-02, TICK-03, TICK-04, TICK-05"
    - path: "apps/api/src/__tests__/ticket-service.test.ts"
      provides: "Ticket service unit test stubs for status machine, activity log"
    - path: "apps/api/src/__tests__/email-inbound.test.ts"
      provides: "Email inbound test stubs for EMAL-03, EMAL-04 (threading + dedup)"
    - path: "apps/api/src/__tests__/notification-service.test.ts"
      provides: "Notification dispatch test stubs for NOTF-04"
    - path: "apps/api/src/__tests__/reports.test.ts"
      provides: "Report CSV export test stubs for REPT-02"
  key_links:
    - from: "apps/api/src/__tests__/*.test.ts"
      to: "apps/api/vitest.config.ts"
      via: "vitest discovers and runs test files"
      pattern: "vitest"
---

<objective>
Create Wave 0 test scaffold files for all core ITSM service domains. These test files establish the behavioral contract before implementation begins, enabling TDD-style development in subsequent plans.

Purpose: Nyquist compliance requires automated verify commands that run actual tests. Without these test files, plans 01-08 can only verify via tsc --noEmit which catches type errors but not behavioral correctness.
Output: 6 test files with describe blocks and pending test cases, plus shared test utilities.
</objective>

<execution_context>
@C:/Users/greiner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/greiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-itsm/03-CONTEXT.md
@.planning/phases/03-core-itsm/03-RESEARCH.md

<interfaces>
From apps/api/vitest.config.ts (existing):
```typescript
// Vitest config already exists from Phase 1
```

From apps/api/src/__tests__/stripe-webhook.test.ts (existing test pattern):
```typescript
// Reference for test structure and mocking patterns
```

From 03-RESEARCH.md Validation Architecture:
- tickets.test.ts -- covers TICK-01, TICK-02, TICK-03, TICK-04, TICK-05
- ticket-service.test.ts -- unit tests for status machine, activity log
- sla-service.test.ts -- covers SLA-02, SLA-04 (already created in Plan 02 Task 1)
- email-inbound.test.ts -- covers EMAL-03, EMAL-04
- notification-service.test.ts -- covers NOTF-04
- reports.test.ts -- covers REPT-02
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test utilities and ticket test scaffolds</name>
  <files>
    apps/api/src/test-utils/setup.ts,
    apps/api/src/__tests__/tickets.test.ts,
    apps/api/src/__tests__/ticket-service.test.ts
  </files>
  <read_first>
    apps/api/vitest.config.ts,
    apps/api/src/__tests__/stripe-webhook.test.ts,
    apps/api/tsconfig.json,
    packages/db/prisma/schema.prisma
  </read_first>
  <action>
1. **Create `apps/api/src/test-utils/setup.ts`**:
   - Export `mockPrisma` -- a vitest mock of the Prisma client with `vi.fn()` stubs for `ticket.create`, `ticket.findMany`, `ticket.findUnique`, `ticket.update`, `ticket.count`, `ticketComment.create`, `ticketActivity.create`, `notification.create`, `notification.updateMany`, `knowledgeArticle.create`, `knowledgeArticle.findMany`, `$transaction`, `$queryRaw`
   - Export `createTestContext(overrides?)` -- returns `{ tenantId: 'test-tenant-id', userId: 'test-user-id', user: { id, email, firstName, lastName, systemRole: 'admin' } }`
   - Export `mockRedis` -- vi.fn() stubs for `sismember`, `sadd`, `expire`

2. **Create `apps/api/src/__tests__/tickets.test.ts`** (integration stubs):
   - `describe('POST /api/v1/tickets', () => { ... })`
     - `it('creates a ticket with sequential TKT-NNNNN number')` -- TICK-01, TICK-02
     - `it('validates title is required and max 500 chars')`
     - `it('applies planGate enforcement on ticket count limit')`
     - `it('auto-assigns to queue default assignee when autoAssign=true')` -- TICK-09
   - `describe('GET /api/v1/tickets', () => { ... })`
     - `it('returns paginated ticket list filtered by tenantId')` -- TICK-07
     - `it('supports search by title and description')`
     - `it('filters by status, priority, assignee, category')`
   - `describe('POST /api/v1/tickets/:id/comments', () => { ... })`
     - `it('creates PUBLIC comment visible to all')` -- TICK-04
     - `it('creates INTERNAL comment visible only to staff')`
     - `it('forces PUBLIC visibility for end_user role')`
     - `it('tracks time spent on comment')` -- TICK-12
   - `describe('POST /api/v1/tickets/:id/attachments', () => { ... })`
     - `it('uploads file to MinIO and creates attachment record')` -- TICK-05
     - `it('rejects files over 25MB')`
   - All tests initially use `it.todo()` or `it.skip()` so the file runs but tests are pending

3. **Create `apps/api/src/__tests__/ticket-service.test.ts`** (unit stubs):
   - `describe('ALLOWED_TRANSITIONS', () => { ... })`
     - `it('allows NEW -> OPEN')` -- TICK-03
     - `it('allows NEW -> IN_PROGRESS')`
     - `it('rejects NEW -> RESOLVED (invalid transition)')`
     - `it('rejects CLOSED -> any (terminal state)')`
     - `it('allows RESOLVED -> OPEN (reopen)')`
   - `describe('createTicket', () => { ... })`
     - `it('generates sequential ticket number within transaction')`
     - `it('creates TicketActivity CREATED record')` -- TICK-06
   - `describe('updateTicket', () => { ... })`
     - `it('sets resolvedAt on status change to RESOLVED')`
     - `it('sets slaPausedAt in customFields on PENDING')`
     - `it('creates TicketActivity FIELD_CHANGED for each changed field')`
   - `describe('addComment', () => { ... })`
     - `it('sets firstResponseAt on first non-requester comment')`
   - All tests initially use `it.todo()`
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter api vitest run src/__tests__/tickets.test.ts src/__tests__/ticket-service.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Test scaffold files exist and run under vitest (all tests pending/todo). Test utilities provide mockPrisma and createTestContext for use across all test files.</done>
</task>

<task type="auto">
  <name>Task 2: Email, notification, and report test scaffolds</name>
  <files>
    apps/api/src/__tests__/email-inbound.test.ts,
    apps/api/src/__tests__/notification-service.test.ts,
    apps/api/src/__tests__/reports.test.ts
  </files>
  <read_first>
    apps/api/src/test-utils/setup.ts,
    apps/api/src/__tests__/tickets.test.ts,
    apps/api/vitest.config.ts
  </read_first>
  <action>
1. **Create `apps/api/src/__tests__/email-inbound.test.ts`**:
   - `describe('isDuplicate', () => { ... })`
     - `it('returns false for new Message-ID and stores it in Redis')` -- EMAL-04
     - `it('returns true for already-seen Message-ID')`
   - `describe('findTicketByHeaders', () => { ... })`
     - `it('matches ticket via In-Reply-To header')` -- EMAL-03
     - `it('matches ticket via References header array')`
     - `it('returns null when no headers match')`
   - `describe('findTicketBySubject', () => { ... })`
     - `it('extracts TKT-XXXXX from subject and finds ticket')`
     - `it('returns null when subject has no ticket reference')`
   - `describe('pollMailbox', () => { ... })`
     - `it('creates ticket from new unread email')`
     - `it('appends comment to existing ticket for reply email')`
     - `it('skips duplicate Message-ID')`
   - All tests use `it.todo()`

2. **Create `apps/api/src/__tests__/notification-service.test.ts`**:
   - `describe('notifyUser', () => { ... })`
     - `it('creates in-app Notification record')` -- NOTF-04
     - `it('enqueues email job when emailData provided')`
     - `it('skips email job when emailData not provided')`
   - `describe('notifyTicketCreated', () => { ... })`
     - `it('notifies assignee when ticket has assignedToId')`
     - `it('does not notify when assignee is the creator')`
   - `describe('notifyTicketCommented', () => { ... })`
     - `it('notifies requester on PUBLIC comment')`
     - `it('notifies assignee on comment (if not the commenter)')`
   - `describe('markAllRead', () => { ... })`
     - `it('marks all unread notifications as read for user')`
   - All tests use `it.todo()`

3. **Create `apps/api/src/__tests__/reports.test.ts`**:
   - `describe('getTicketReport', () => { ... })`
     - `it('returns CSV format with correct headers')` -- REPT-02
     - `it('returns JSON format with ticket data')`
     - `it('caps results at 5000 records')`
     - `it('filters by date range')`
   - `describe('getSlaComplianceReport', () => { ... })`
     - `it('calculates correct compliance rate')` -- REPT-04
     - `it('returns per-priority breakdown')`
   - `describe('getDashboardStats', () => { ... })`
     - `it('returns total, open, resolved today, and overdue counts')` -- REPT-01
   - All tests use `it.todo()`
  </action>
  <verify>
    <automated>cd "C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application" && pnpm --filter api vitest run src/__tests__/email-inbound.test.ts src/__tests__/notification-service.test.ts src/__tests__/reports.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All Wave 0 test scaffold files exist and run under vitest. Email inbound tests cover threading and dedup behaviors. Notification tests cover dispatch orchestrator. Report tests cover CSV export and SLA compliance calculation.</done>
</task>

</tasks>

<verification>
- `pnpm --filter api vitest run src/__tests__/ --reporter=verbose` runs all test files without errors
- All test files contain `describe` blocks with `it.todo()` tests
- Test utilities in test-utils/setup.ts export mockPrisma and createTestContext
- sla-service.test.ts already created in Plan 02 Task 1 (not duplicated here)
</verification>

<success_criteria>
- 5 new test files exist under apps/api/src/__tests__/
- Test utility file exists at apps/api/src/test-utils/setup.ts
- vitest discovers and runs all files (with pending tests reporting as todo)
- Combined with Plan 02's sla-service.test.ts, all 6 RESEARCH.md Wave 0 gaps are covered
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-itsm/03-10-SUMMARY.md`
</output>
