---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/seed.ts
  - packages/db/src/index.ts
  - packages/db/src/client.ts
  - packages/db/src/extensions/tenant.ts
  - packages/types/src/index.ts
  - packages/types/src/auth.ts
  - packages/types/src/tenant.ts
  - packages/types/src/common.ts
autonomous: true
requirements: [FNDN-02, FNDN-03, FNDN-07, TNCY-01, TNCY-02, TNCY-03, TNCY-05]
must_haves:
  truths:
    - "Prisma schema defines all 61 models with tenantId on every tenant-scoped table"
    - "prisma migrate dev creates all tables successfully"
    - "prisma db seed creates default tenant, system roles, and test users"
    - "Prisma client extension automatically injects tenantId on queries"
    - "Tenant B's session cannot retrieve Tenant A's records through the extension"
    - "Zod schemas for auth and tenant inputs/outputs are exported from packages/types"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "All 61 Prisma models"
      contains: "model Tenant"
      min_lines: 800
    - path: "packages/db/src/extensions/tenant.ts"
      provides: "Prisma client extension for tenant scoping"
      exports: ["withTenantScope"]
    - path: "packages/db/prisma/seed.ts"
      provides: "Database seeding script"
      contains: "admin@msp.local"
    - path: "packages/types/src/auth.ts"
      provides: "Zod schemas for auth"
      contains: "loginSchema"
  key_links:
    - from: "packages/db/src/client.ts"
      to: "packages/db/src/extensions/tenant.ts"
      via: "import withTenantScope"
      pattern: "withTenantScope"
    - from: "packages/db/prisma/seed.ts"
      to: "packages/db/src/client.ts"
      via: "PrismaClient import"
      pattern: "PrismaClient"
---

<objective>
Define the complete Prisma 7 schema with all 61 models, create the tenant-scoping client extension, write the database seed script, and create the shared Zod types package.

Purpose: The data model is the foundation every other plan builds on. Getting this right prevents migration churn across 50+ tables in later phases.
Output: A complete Prisma schema, working migrations, seeded database, tenant isolation extension, and shared type definitions.
</objective>

<execution_context>
@C:/Users/greiner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/greiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@DOCUMENTATION .md (Section 3: Data Model — lines 145-270)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define complete Prisma 7 schema with all 61 models</name>
  <files>
    packages/db/prisma/schema.prisma,
    packages/db/src/index.ts,
    packages/db/src/client.ts,
    packages/db/src/extensions/tenant.ts
  </files>
  <read_first>
    DOCUMENTATION .md,
    .planning/phases/01-foundation/01-RESEARCH.md,
    packages/db/package.json
  </read_first>
  <action>
Create `packages/db/prisma/schema.prisma` with the complete data model. Use PostgreSQL provider. The full model inventory from DOCUMENTATION.md Section 3 and the RESEARCH.md model list:

**Generator and datasource:**
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

**Enums to define (from DOCUMENTATION.md Section 3 Key Enums):**
- `TenantType`: MSP, ENTERPRISE, B2C
- `TenantStatus`: ACTIVE, SUSPENDED, DELETED
- `SubscriptionPlanTier`: STARTER, PROFESSIONAL, BUSINESS, ENTERPRISE
- `SubscriptionStatus`: TRIALING, ACTIVE, PAST_DUE, CANCELED, SUSPENDED
- `UserStatus`: ACTIVE, INACTIVE, SUSPENDED
- `TicketStatus`: NEW, OPEN, IN_PROGRESS, PENDING, RESOLVED, CLOSED, CANCELLED
- `TicketType`: INCIDENT, SERVICE_REQUEST, PROBLEM
- `TicketPriority`: LOW, MEDIUM, HIGH, CRITICAL
- `CommentVisibility`: PUBLIC, INTERNAL
- `ChangeStatus`: NEW, ASSESSMENT, APPROVAL_PENDING, APPROVED, REJECTED, SCHEDULED, IMPLEMENTING, REVIEW, COMPLETED, CANCELLED
- `ChangeType`: STANDARD, NORMAL, EMERGENCY
- `ApprovalStatus`: PENDING, APPROVED, REJECTED, CANCELLED
- `RiskLevel`: LOW, MEDIUM, HIGH, CRITICAL
- `AssetStatus`: IN_STOCK, DEPLOYED, IN_REPAIR, RETIRED, DISPOSED
- `AgentStatus`: ENROLLING, ACTIVE, OFFLINE, SUSPENDED
- `AgentPlatform`: WINDOWS, LINUX, MACOS
- `ArticleStatus`: DRAFT, IN_REVIEW, PUBLISHED, RETIRED
- `ArticleVisibility`: PUBLIC, INTERNAL
- `NotificationType`: TICKET_ASSIGNED, TICKET_UPDATED, TICKET_COMMENTED, TICKET_RESOLVED, TICKET_CREATED, SLA_WARNING, SLA_BREACH, CHANGE_APPROVAL, CHANGE_UPDATED, MENTION, SYSTEM, CAB_INVITATION
- `AuditAction`: CREATE, UPDATE, DELETE, LOGIN, LOGOUT, APPROVE, REJECT, ASSIGN, ESCALATE
- `DevicePlatform`: IOS, ANDROID
- `CmdbCiStatus`: ACTIVE, INACTIVE, DECOMMISSIONED, PLANNED
- `CmdbCiType`: SERVER, WORKSTATION, NETWORK_DEVICE, SOFTWARE, SERVICE, DATABASE, VIRTUAL_MACHINE, CONTAINER, OTHER
- `CmdbCiEnvironment`: PRODUCTION, STAGING, DEV, DR
- `CmdbRelationshipType`: DEPENDS_ON, HOSTS, CONNECTS_TO, RUNS_ON, BACKS_UP, VIRTUALIZES, MEMBER_OF
- `CmdbChangeType`: CREATED, UPDATED, DELETED
- `CmdbChangedBy`: USER, AGENT, IMPORT
- `CmdbTicketLinkType`: AFFECTED, RELATED, CAUSED_BY
- `CABAttendeeRole`: CHAIRPERSON, MEMBER, OBSERVER
- `RSVPStatus`: PENDING, ACCEPTED, DECLINED, TENTATIVE
- `CABOutcome`: APPROVED, REJECTED, DEFERRED, NEEDS_MORE_INFO
- `CABMeetingStatus`: SCHEDULED, IN_PROGRESS, COMPLETED, CANCELLED
- `ApplicationType`: WEB, MOBILE, DESKTOP, API, SERVICE, DATABASE_APP, MIDDLEWARE, INFRASTRUCTURE, OTHER
- `ApplicationStatus`: ACTIVE, INACTIVE, DECOMMISSIONED, PLANNED, IN_DEVELOPMENT
- `CriticalityLevel`: LOW, MEDIUM, HIGH, CRITICAL
- `HostingModel`: ON_PREMISE, CLOUD, HYBRID, SAAS
- `LifecycleStage`: PLANNING, DEVELOPMENT, PRODUCTION, RETIREMENT
- `DependencyType`: DATA_FLOW, API_CALL, SHARED_DATABASE, AUTHENTICATION, FILE_TRANSFER, MESSAGE_QUEUE, OTHER
- `DocumentType`: ARCHITECTURE, API_SPEC, RUNBOOK, SLA_DOC, SECURITY, COMPLIANCE, USER_GUIDE, ADMIN_GUIDE, RELEASE_NOTES, DEPLOYMENT, OTHER
- `AppAssetRelationship`: RUNS_ON, HOSTED_BY, USES
- `WebhookEventType`: TICKET_CREATED, TICKET_UPDATED, TICKET_RESOLVED, TICKET_CLOSED, COMMENT_ADDED, CHANGE_CREATED, CHANGE_APPROVED, SLA_BREACH
- `AlertChannelType`: EMAIL, SMS, SLACK, TEAMS

**Models (61 total) — every tenant-scoped model MUST have:**
```prisma
tenantId  String @db.Uuid
tenant    Tenant @relation(fields: [tenantId], references: [id])
```
Plus `@@index([tenantId])` and commonly `@@index([tenantId, status])`, `@@index([tenantId, createdAt])`.

Every model has `id String @id @default(uuid()) @db.Uuid`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`.

**GLOBAL models (NO tenantId):** Tenant, OwnerUser, OwnerSession, SubscriptionPlan, TenantSubscription, TenantUsageSnapshot, OwnerNote

**Core Tenancy & Identity (8 models):**
1. `Tenant` — name, slug (unique), type TenantType, status TenantStatus default ACTIVE, subdomain (unique, optional), backendUrl (optional), settings Json?, plan SubscriptionPlanTier default STARTER, planLimitsJson Json?, maxUsers Int default 5, maxAgents Int default 0, maxSites Int default 1, trialEndsAt DateTime?, suspendedAt DateTime?, deletedAt DateTime?. Relations to users, customerOrgs, etc.
2. `CustomerOrganization` — name, slug, primaryContactName, primaryContactEmail, primaryContactPhone, address, city, state, postalCode, country. tenantId scoped. @@unique([tenantId, slug])
3. `User` — email, passwordHash, firstName, lastName, displayName, phone, jobTitle, department, status UserStatus default ACTIVE, notificationPreferences Json?, siteId (optional FK to Site). tenantId scoped. @@unique([tenantId, email])
4. `UserGroup` — name, email (optional), description. tenantId scoped.
5. `Role` — name, slug, permissions Json (String array), isSystemRole Boolean default false. tenantId scoped. @@unique([tenantId, slug])
6. `UserRole` — userId FK, roleId FK, customerOrganizationId (optional FK). tenantId scoped. @@unique([userId, roleId])
7. `Session` — sessionToken (unique), userId FK, expiresAt DateTime. tenantId scoped.
8. `ApiKey` — name, keyHash, keyPrefix (8 chars), scopes Json (String array), rateLimit Int default 100, isActive Boolean default true, lastUsedAt DateTime?, expiresAt DateTime?. tenantId scoped. @@index([keyHash])

**Service Desk (9 models):**
9. `Ticket` — ticketNumber String, type TicketType, title, description (Text), priority TicketPriority default MEDIUM, impact String?, urgency String?, status TicketStatus default NEW, resolution String? (Text), assigneeId (optional FK User), assigneeGroupId (optional FK UserGroup), categoryId (optional FK Category), queueId (optional FK Queue), reporterId FK User, customerOrganizationId (optional FK), slaId (optional FK SLA), slaResponseDue DateTime?, slaResolutionDue DateTime?, slaResponseMet Boolean?, slaResolutionMet Boolean?, respondedAt DateTime?, resolvedAt DateTime?, closedAt DateTime?. tenantId scoped. @@unique([tenantId, ticketNumber]), @@index([tenantId, status]), @@index([tenantId, assigneeId]), @@index([tenantId, priority])
10. `TicketComment` — ticketId FK, authorId FK User, content Text, visibility CommentVisibility default PUBLIC, timeSpentMinutes Int default 0. tenantId scoped.
11. `TicketAttachment` — ticketId FK, uploadedById FK User, filename, mimeType, fileSize BigInt, storagePath. tenantId scoped.
12. `TicketActivity` — ticketId FK, userId FK User (optional), activityType String, fieldName String?, oldValue String?, newValue String?. tenantId scoped. @@index([tenantId, ticketId])
13. `TicketKnowledgeArticle` — ticketId FK, knowledgeArticleId FK. tenantId scoped. @@unique([ticketId, knowledgeArticleId])
14. `Queue` — name, description, autoAssign Boolean default false, defaultAssigneeId (optional FK User), assignmentRules Json?. tenantId scoped.
15. `SLA` — name, description, p1ResponseMinutes Int, p1ResolutionMinutes Int, p2ResponseMinutes Int, p2ResolutionMinutes Int, p3ResponseMinutes Int, p3ResolutionMinutes Int, p4ResponseMinutes Int, p4ResolutionMinutes Int, businessHoursStart String default "09:00", businessHoursEnd String default "17:00", businessDays Json default "[1,2,3,4,5]", timezone String default "UTC", isDefault Boolean default false. tenantId scoped.
16. `Category` — name, slug, icon String?, color String?, description String?, parentId (optional self-FK), userGroupId (optional FK). tenantId scoped. @@unique([tenantId, slug])
17. `EmailAccount` — name, email, smtpHost, smtpPort Int, smtpUser, smtpPasswordEncrypted, imapHost String?, imapPort Int?, imapUser String?, imapPasswordEncrypted String?, protocol String default "SMTP", isDefault Boolean default false, pollInterval Int default 5, lastPolledAt DateTime?, createTicketOnReceive Boolean default true. tenantId scoped.

**Change Management (8 models):**
18. `Change` — changeNumber, type ChangeType default NORMAL, title, description Text, implementationPlan Text?, backoutPlan Text?, testingPlan Text?, riskLevel RiskLevel default MEDIUM, status ChangeStatus default NEW, requesterId FK User, assigneeId (optional FK User), categoryId (optional FK Category), scheduledStart DateTime?, scheduledEnd DateTime?, actualStart DateTime?, actualEnd DateTime?, riskScore Int?. tenantId scoped. @@unique([tenantId, changeNumber]), @@index([tenantId, status])
19. `ChangeApproval` — changeId FK, approverId FK User, status ApprovalStatus default PENDING, sequenceOrder Int default 0, comments Text?, decidedAt DateTime?. tenantId scoped.
20. `ChangeActivity` — changeId FK, userId (optional FK User), activityType String, fieldName String?, oldValue String?, newValue String?. tenantId scoped.
21. `ChangeApplication` — changeId FK, applicationId FK. tenantId scoped. @@unique([changeId, applicationId])
22. `ChangeAsset` — changeId FK, assetId FK. tenantId scoped. @@unique([changeId, assetId])
23. `CABMeeting` — title, description Text?, scheduledFor DateTime, durationMinutes Int default 60, location String?, meetingUrl String?, status CABMeetingStatus default SCHEDULED, organizerId FK User. tenantId scoped.
24. `CABMeetingAttendee` — meetingId FK CABMeeting, userId FK User, role CABAttendeeRole default MEMBER, rsvpStatus RSVPStatus default PENDING. tenantId scoped. @@unique([meetingId, userId])
25. `CABMeetingChange` — meetingId FK CABMeeting, changeId FK Change, agendaOrder Int default 0, outcome CABOutcome?, notes Text?. tenantId scoped. @@unique([meetingId, changeId])

**Knowledge Management (1 model):**
26. `KnowledgeArticle` — articleNumber, title, summary Text?, content Text, tags String[], visibility ArticleVisibility default PUBLIC, status ArticleStatus default DRAFT, authorId FK User, viewCount Int default 0, helpfulCount Int default 0, notHelpfulCount Int default 0. tenantId scoped. @@unique([tenantId, articleNumber])

**Asset & Endpoint Management (3 models):**
27. `Asset` — assetTag, serialNumber String?, manufacturer String?, model String?, status AssetStatus default IN_STOCK, assignedUserId (optional FK User), siteId (optional FK Site), purchaseDate DateTime?, purchaseCost Decimal?, warrantyExpiryDate DateTime?, hostname String?, osName String?, osVersion String?, cpuModel String?, cpuCores Int?, memoryMb Int?, diskJson Json?, networkInterfacesJson Json?, softwareInventoryJson Json?. tenantId scoped. @@unique([tenantId, assetTag])
28. `Site` — name, address String?, city String?, state String?, postalCode String?, country String?, primaryContactName String?, primaryContactEmail String?, primaryContactPhone String?. tenantId scoped.
29. `BusinessUnit` — name, code String?, description String?, managerName String?, managerEmail String?. tenantId scoped.

**Application Portfolio (5 models):**
30. `Application` — name, type ApplicationType default WEB, status ApplicationStatus default ACTIVE, criticality CriticalityLevel default MEDIUM, hostingModel HostingModel default CLOUD, techStack String[], authMethod String?, dataClassification String?, annualCost Decimal?, rpo String?, rto String?, lifecycleStage LifecycleStage default PRODUCTION, strategicRating Int?, description Text?, ownerId (optional FK User), businessUnitId (optional FK BusinessUnit). tenantId scoped.
31. `ApplicationDependency` — sourceApplicationId FK Application, targetApplicationId FK Application, dependencyType DependencyType default API_CALL, description String?. tenantId scoped. @@unique([sourceApplicationId, targetApplicationId])
32. `ApplicationDocument` — applicationId FK, title, documentType DocumentType, url String, description String?. tenantId scoped.
33. `ApplicationActivity` — applicationId FK, userId (optional FK User), activityType String, fieldName String?, oldValue String?, newValue String?. tenantId scoped.
34. `ApplicationAsset` — applicationId FK, assetId FK, relationshipType AppAssetRelationship default RUNS_ON, isPrimary Boolean default false. tenantId scoped. @@unique([applicationId, assetId])

**Agent & Inventory (4 models):**
35. `Agent` — agentKey String (unique), hostname, platform AgentPlatform, platformVersion String?, agentVersion String?, status AgentStatus default ENROLLING, lastHeartbeat DateTime?, ipAddress String?, assetId (optional FK Asset). tenantId scoped. @@index([agentKey])
36. `AgentEnrollmentToken` — tokenHash String (unique), name, scopes Json?, maxEnrollments Int default 1, usedEnrollments Int default 0, expiresAt DateTime?. tenantId scoped.
37. `InventorySnapshot` — agentId FK, osName String?, osVersion String?, cpuModel String?, cpuCores Int?, memoryMb Int?, diskJson Json?, networkInterfacesJson Json?, installedSoftwareJson Json?, localUsersJson Json?, rawData Json?. tenantId scoped. @@index([tenantId, agentId])
38. `MetricSample` — agentId FK, metricType String, metricName String, value Float, unit String?, tags Json?, timestamp DateTime. tenantId scoped. @@index([tenantId, agentId, timestamp])

**CMDB (5 models):**
39. `CmdbCategory` — name, slug, icon String?, color String?, parentId (optional self-FK), description String?. tenantId scoped. @@unique([tenantId, slug])
40. `CmdbConfigurationItem` — ciNumber, name, type CmdbCiType, status CmdbCiStatus default ACTIVE, environment CmdbCiEnvironment?, categoryId (optional FK CmdbCategory), assetId (optional FK Asset), agentId (optional FK Agent), ownerId (optional FK User), siteId (optional FK Site), attributesJson Json?, description Text?, discoveredAt DateTime?, lastSeenAt DateTime?. tenantId scoped. @@unique([tenantId, ciNumber]), @@index([tenantId, type]), @@index([tenantId, status])
41. `CmdbRelationship` — sourceId FK CmdbConfigurationItem, targetId FK CmdbConfigurationItem, relationshipType CmdbRelationshipType, description String?, isDiscovered Boolean default false. tenantId scoped. @@unique([sourceId, targetId, relationshipType])
42. `CmdbChangeRecord` — ciId FK CmdbConfigurationItem, changeType CmdbChangeType, fieldName String?, oldValue String?, newValue String?, changedBy CmdbChangedBy default USER, agentId (optional FK Agent), userId (optional FK User). tenantId scoped. @@index([tenantId, ciId])
43. `CmdbTicketLink` — ciId FK CmdbConfigurationItem, ticketId FK Ticket, linkType CmdbTicketLinkType default AFFECTED. tenantId scoped. @@unique([ciId, ticketId])

**Owner Admin & Billing (6 GLOBAL models — NO tenantId):**
44. `OwnerUser` — email (unique), passwordHash, totpSecret String?, totpEnabled Boolean default false, lastLoginAt DateTime?. GLOBAL.
45. `OwnerSession` — sessionToken (unique), ownerUserId FK OwnerUser, expiresAt DateTime, ipAddress String?, userAgent String?. GLOBAL.
46. `SubscriptionPlan` — name SubscriptionPlanTier (unique), displayName, monthlyPriceUsd Decimal, annualPriceUsd Decimal, limitsJson Json, stripePriceIdMonthly String?, stripePriceIdAnnual String?, isPublic Boolean default true, features String[]. GLOBAL.
47. `TenantSubscription` — tenantId FK Tenant (unique), planId FK SubscriptionPlan, stripeCustomerId String?, stripeSubscriptionId String?, status SubscriptionStatus default TRIALING, currentPeriodStart DateTime?, currentPeriodEnd DateTime?, trialStart DateTime?, trialEnd DateTime?, cancelAtPeriodEnd Boolean default false. GLOBAL.
48. `TenantUsageSnapshot` — tenantId FK Tenant, snapshotDate DateTime, activeUsers Int default 0, activeAgents Int default 0, ticketCount Int default 0, storageBytes BigInt default 0. GLOBAL. @@index([tenantId, snapshotDate])
49. `OwnerNote` — tenantId FK Tenant, ownerUserId FK OwnerUser, content Text, isPrivate Boolean default true. GLOBAL.

**Supporting (12 models, all tenant-scoped except where noted):**
50. `Vendor` — name, contactName String?, contactEmail String?, contactPhone String?, website String?, address String?. tenantId scoped.
51. `Contract` — name, vendorId FK Vendor, startDate DateTime, endDate DateTime?, value Decimal?, status String default "ACTIVE", slaId (optional FK SLA), description Text?, complianceNotes Text?. tenantId scoped.
52. `ContractAsset` — contractId FK Contract, assetId FK Asset. tenantId scoped. @@unique([contractId, assetId])
53. `AuditLog` — userId (optional FK User), action AuditAction, entityType String, entityId String, details Json?, ipAddress String?. tenantId scoped. @@index([tenantId, entityType, entityId])
54. `Notification` — userId FK User, type NotificationType, title, message Text?, entityType String?, entityId String?, isRead Boolean default false, readAt DateTime?. tenantId scoped. @@index([tenantId, userId, isRead])
55. `DeviceToken` — userId FK User, token String, deviceId String, platform DevicePlatform, appVersion String?. tenantId scoped. @@unique([token])
56. `EmailTemplate` — name, slug (unique per tenant), subject, bodyHtml Text, bodyText Text?, variables String[]. tenantId scoped. @@unique([tenantId, slug])
57. `AlertConfiguration` — name, channelType AlertChannelType, config Json, isActive Boolean default true. tenantId scoped.
58. `ScheduledReport` — name, reportType String, parameters Json?, schedule String, lastRunAt DateTime?, nextRunAt DateTime?, recipientEmails String[], format String default "CSV". tenantId scoped.
59. `Webhook` — name, url String, events WebhookEventType[], secret String, isActive Boolean default true, lastDeliveryAt DateTime?. tenantId scoped.
60. `WebhookDelivery` — webhookId FK Webhook, event WebhookEventType, payload Json, responseStatus Int?, responseBody String?, deliveredAt DateTime?, attempts Int default 0, nextRetryAt DateTime?. tenantId scoped. @@index([tenantId, webhookId])
61. `PasswordResetToken` — token String (unique, hashed), userId FK User, expiresAt DateTime, usedAt DateTime?. tenantId scoped.

Also add a M2M table for UserGroup members:
- `UserGroupMember` — userGroupId FK, userId FK. tenantId scoped. @@unique([userGroupId, userId])

**Create the Prisma tenant-scoping client extension:**

`packages/db/src/extensions/tenant.ts`:
```typescript
import { Prisma } from '@prisma/client';

const GLOBAL_MODELS = new Set([
  'Tenant',
  'OwnerUser',
  'OwnerSession',
  'SubscriptionPlan',
  'TenantSubscription',
  'TenantUsageSnapshot',
  'OwnerNote',
]);

export function withTenantScope(tenantId: string) {
  return Prisma.defineExtension((client) =>
    client.$extends({
      query: {
        $allModels: {
          async $allOperations({ model, operation, args, query }) {
            if (GLOBAL_MODELS.has(model!)) {
              return query(args);
            }

            if (['create', 'createMany', 'createManyAndReturn'].includes(operation)) {
              if (operation === 'createMany') {
                const data = Array.isArray(args.data) ? args.data : [args.data];
                args.data = data.map((d: any) => ({ ...d, tenantId }));
              } else {
                args.data = { ...args.data, tenantId };
              }
            }

            if (['findFirst', 'findMany', 'findUnique', 'findFirstOrThrow', 'findUniqueOrThrow', 'count', 'aggregate', 'groupBy'].includes(operation)) {
              args.where = { ...args.where, tenantId };
            }

            if (['update', 'updateMany', 'delete', 'deleteMany', 'upsert'].includes(operation)) {
              if (operation === 'upsert') {
                args.where = { ...args.where, tenantId };
                args.create = { ...args.create, tenantId };
              } else {
                args.where = { ...args.where, tenantId };
              }
            }

            return query(args);
          },
        },
      },
    })
  );
}
```

`packages/db/src/client.ts`:
```typescript
import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as { prisma: PrismaClient | undefined };

export const prisma = globalForPrisma.prisma ?? new PrismaClient({ log: ['error', 'warn'] });

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;

export { PrismaClient } from '@prisma/client';
export { withTenantScope } from './extensions/tenant.js';
```

`packages/db/src/index.ts`:
```typescript
export { prisma, PrismaClient, withTenantScope } from './client.js';
```

After creating the schema, run:
```bash
cd packages/db && npx prisma generate
```
  </action>
  <verify>
    <automated>cd /c/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application && grep -c "^model " packages/db/prisma/schema.prisma && grep -c "tenantId" packages/db/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/prisma/schema.prisma contains at least 55 "model " declarations
    - packages/db/prisma/schema.prisma contains "tenantId" on every tenant-scoped model
    - packages/db/prisma/schema.prisma contains "@@index([tenantId])" on tenant-scoped models
    - packages/db/prisma/schema.prisma does NOT contain tenantId on Tenant, OwnerUser, OwnerSession, SubscriptionPlan, TenantSubscription, TenantUsageSnapshot, OwnerNote models
    - packages/db/src/extensions/tenant.ts contains "withTenantScope" export and "GLOBAL_MODELS" set
    - packages/db/src/client.ts contains "PrismaClient" and imports "withTenantScope"
    - packages/db/src/index.ts exports "prisma", "withTenantScope"
    - npx prisma generate succeeds (exit 0)
  </acceptance_criteria>
  <done>All 61+ Prisma models defined with tenantId scoping, indexes, and unique constraints. Tenant extension created. Prisma client generates without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Zod types package and database seed script</name>
  <files>
    packages/types/src/auth.ts,
    packages/types/src/tenant.ts,
    packages/types/src/common.ts,
    packages/types/src/index.ts,
    packages/db/prisma/seed.ts
  </files>
  <read_first>
    packages/db/prisma/schema.prisma,
    packages/types/package.json,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
**Zod schemas (`packages/types/src/`):**

`common.ts`:
```typescript
import { z } from 'zod';

export const uuidSchema = z.string().uuid();
export const paginationSchema = z.object({
  page: z.coerce.number().int().min(1).default(1),
  limit: z.coerce.number().int().min(1).max(100).default(20),
});
export const sortSchema = z.object({
  sortBy: z.string().default('createdAt'),
  sortOrder: z.enum(['asc', 'desc']).default('desc'),
});
export const apiErrorSchema = z.object({
  error: z.string(),
  message: z.string().optional(),
  statusCode: z.number(),
});
```

`auth.ts`:
```typescript
import { z } from 'zod';

export const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
});
export type LoginInput = z.infer<typeof loginSchema>;

export const registerSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8).max(128),
  firstName: z.string().min(1).max(100),
  lastName: z.string().min(1).max(100),
});
export type RegisterInput = z.infer<typeof registerSchema>;

export const refreshTokenSchema = z.object({
  refreshToken: z.string(),
});

export const passwordResetRequestSchema = z.object({
  email: z.string().email(),
});

export const passwordResetSchema = z.object({
  token: z.string(),
  password: z.string().min(8).max(128),
});

export const changePasswordSchema = z.object({
  currentPassword: z.string(),
  newPassword: z.string().min(8).max(128),
});

export const jwtPayloadSchema = z.object({
  userId: z.string().uuid(),
  tenantId: z.string().uuid(),
  email: z.string().email(),
  roles: z.array(z.string()),
  type: z.enum(['access', 'refresh']).default('access'),
});
export type JwtPayload = z.infer<typeof jwtPayloadSchema>;

export const apiKeyCreateSchema = z.object({
  name: z.string().min(1).max(100),
  scopes: z.array(z.string()),
  rateLimit: z.number().int().min(1).max(10000).default(100),
  expiresAt: z.string().datetime().optional(),
});
```

`tenant.ts`:
```typescript
import { z } from 'zod';

export const tenantTypeSchema = z.enum(['MSP', 'ENTERPRISE', 'B2C']);
export const subscriptionPlanTierSchema = z.enum(['STARTER', 'PROFESSIONAL', 'BUSINESS', 'ENTERPRISE']);

export const createTenantSchema = z.object({
  name: z.string().min(2).max(100),
  slug: z.string().min(2).max(50).regex(/^[a-z0-9-]+$/),
  type: tenantTypeSchema.default('MSP'),
  subdomain: z.string().min(2).max(50).regex(/^[a-z0-9-]+$/).optional(),
});

export const tenantSettingsSchema = z.object({
  timezone: z.string().default('UTC'),
  dateFormat: z.string().default('YYYY-MM-DD'),
  timeFormat: z.enum(['12h', '24h']).default('24h'),
}).passthrough();
```

`index.ts`:
```typescript
export * from './auth.js';
export * from './tenant.js';
export * from './common.js';
```

**Seed script (`packages/db/prisma/seed.ts`):**

Uses @node-rs/bcrypt for password hashing. Must be added to packages/db devDependencies: `@node-rs/bcrypt`, `tsx`.

```typescript
import { PrismaClient } from '@prisma/client';
import { hashSync } from '@node-rs/bcrypt';

const prisma = new PrismaClient();

async function main() {
  console.log('Seeding database...');

  // 1. Default MSP tenant
  const tenant = await prisma.tenant.upsert({
    where: { slug: 'msp-default' },
    update: {},
    create: {
      name: 'Default MSP',
      slug: 'msp-default',
      type: 'MSP',
      status: 'ACTIVE',
      subdomain: 'default',
      plan: 'PROFESSIONAL',
      maxUsers: 50,
      maxAgents: 100,
      maxSites: 10,
    },
  });
  console.log(`Tenant: ${tenant.name} (${tenant.id})`);

  // 2. System roles
  const roleDefinitions = [
    { name: 'Admin', slug: 'admin', permissions: ['*'], isSystemRole: true },
    { name: 'MSP Admin', slug: 'msp_admin', permissions: ['tickets.*', 'users.*', 'settings.*', 'knowledge.*', 'assets.*', 'changes.*', 'reports.*'], isSystemRole: true },
    { name: 'Agent', slug: 'agent', permissions: ['tickets.read', 'tickets.create', 'tickets.update', 'tickets.comment', 'knowledge.read', 'assets.read'], isSystemRole: true },
    { name: 'End User', slug: 'end_user', permissions: ['tickets.create', 'tickets.read.own', 'tickets.comment.own', 'knowledge.read'], isSystemRole: true },
  ];

  const roles: Record<string, any> = {};
  for (const roleDef of roleDefinitions) {
    const role = await prisma.role.upsert({
      where: { tenantId_slug: { tenantId: tenant.id, slug: roleDef.slug } },
      update: { permissions: roleDef.permissions },
      create: { ...roleDef, tenantId: tenant.id },
    });
    roles[roleDef.slug] = role;
    console.log(`Role: ${role.name} (${role.id})`);
  }

  // 3. Test users
  const testUsers = [
    { email: 'admin@msp.local', password: 'Admin123!', firstName: 'MSP', lastName: 'Admin', roleSlug: 'admin' },
    { email: 'agent@msp.local', password: 'Agent123!', firstName: 'Test', lastName: 'Agent', roleSlug: 'agent' },
    { email: 'user@customer.local', password: 'User123!', firstName: 'Customer', lastName: 'User', roleSlug: 'end_user' },
  ];

  for (const u of testUsers) {
    const passwordHash = hashSync(u.password, 10);
    const user = await prisma.user.upsert({
      where: { tenantId_email: { tenantId: tenant.id, email: u.email } },
      update: {},
      create: {
        email: u.email,
        passwordHash,
        firstName: u.firstName,
        lastName: u.lastName,
        displayName: `${u.firstName} ${u.lastName}`,
        tenantId: tenant.id,
        status: 'ACTIVE',
      },
    });
    await prisma.userRole.upsert({
      where: { userId_roleId: { userId: user.id, roleId: roles[u.roleSlug].id } },
      update: {},
      create: { userId: user.id, roleId: roles[u.roleSlug].id, tenantId: tenant.id },
    });
    console.log(`User: ${u.email} (${user.id}) -> role: ${u.roleSlug}`);
  }

  // 4. Default SLA policies
  const defaultSla = await prisma.sLA.upsert({
    where: { id: '00000000-0000-0000-0000-000000000001' },
    update: {},
    create: {
      id: '00000000-0000-0000-0000-000000000001',
      name: 'Default SLA',
      description: 'Default SLA policy for all tickets',
      p1ResponseMinutes: 15,
      p1ResolutionMinutes: 60,
      p2ResponseMinutes: 30,
      p2ResolutionMinutes: 240,
      p3ResponseMinutes: 120,
      p3ResolutionMinutes: 480,
      p4ResponseMinutes: 480,
      p4ResolutionMinutes: 2880,
      isDefault: true,
      tenantId: tenant.id,
    },
  });
  console.log(`SLA: ${defaultSla.name} (${defaultSla.id})`);

  // 5. Default categories
  const categories = [
    { name: 'Hardware', slug: 'hardware', icon: 'mdiDesktopClassic', color: '#2196F3' },
    { name: 'Software', slug: 'software', icon: 'mdiApplication', color: '#4CAF50' },
    { name: 'Network', slug: 'network', icon: 'mdiLan', color: '#FF9800' },
    { name: 'Account', slug: 'account', icon: 'mdiAccount', color: '#9C27B0' },
    { name: 'Other', slug: 'other', icon: 'mdiHelpCircle', color: '#607D8B' },
  ];

  for (const cat of categories) {
    await prisma.category.upsert({
      where: { tenantId_slug: { tenantId: tenant.id, slug: cat.slug } },
      update: {},
      create: { ...cat, tenantId: tenant.id },
    });
    console.log(`Category: ${cat.name}`);
  }

  // 6. Default subscription plans (GLOBAL — no tenantId)
  const plans = [
    { name: 'STARTER' as const, displayName: 'Starter', monthlyPriceUsd: 29, annualPriceUsd: 290, limitsJson: { maxUsers: 5, maxAgents: 10, maxSites: 1, maxTicketsPerMonth: 100 }, features: ['tickets', 'knowledge'], isPublic: true },
    { name: 'PROFESSIONAL' as const, displayName: 'Professional', monthlyPriceUsd: 79, annualPriceUsd: 790, limitsJson: { maxUsers: 25, maxAgents: 50, maxSites: 5, maxTicketsPerMonth: 1000 }, features: ['tickets', 'knowledge', 'changes', 'assets', 'api'], isPublic: true },
    { name: 'BUSINESS' as const, displayName: 'Business', monthlyPriceUsd: 149, annualPriceUsd: 1490, limitsJson: { maxUsers: 100, maxAgents: 200, maxSites: 20, maxTicketsPerMonth: 5000 }, features: ['tickets', 'knowledge', 'changes', 'assets', 'api', 'cmdb', 'webhooks', 'reports'], isPublic: true },
    { name: 'ENTERPRISE' as const, displayName: 'Enterprise', monthlyPriceUsd: 299, annualPriceUsd: 2990, limitsJson: { maxUsers: -1, maxAgents: -1, maxSites: -1, maxTicketsPerMonth: -1 }, features: ['tickets', 'knowledge', 'changes', 'assets', 'api', 'cmdb', 'webhooks', 'reports', 'mobile', 'sso', 'multi_tenant'], isPublic: true },
  ];

  for (const plan of plans) {
    await prisma.subscriptionPlan.upsert({
      where: { name: plan.name },
      update: {},
      create: plan,
    });
    console.log(`Plan: ${plan.displayName}`);
  }

  // 7. Default customer organization (for MSP model)
  await prisma.customerOrganization.upsert({
    where: { id: '00000000-0000-0000-0000-000000000002' },
    update: {},
    create: {
      id: '00000000-0000-0000-0000-000000000002',
      name: 'Sample Customer',
      slug: 'sample-customer',
      primaryContactName: 'Customer User',
      primaryContactEmail: 'user@customer.local',
      tenantId: tenant.id,
    },
  });
  console.log('Customer Organization: Sample Customer');

  console.log('Seeding complete!');
}

main()
  .then(() => prisma.$disconnect())
  .catch((e) => {
    console.error('Seed error:', e);
    prisma.$disconnect();
    process.exit(1);
  });
```

Add `@node-rs/bcrypt` and `tsx` to packages/db devDependencies, then run `pnpm install` in the root.

Ensure packages/db/package.json has the seed configuration:
```json
"prisma": {
  "seed": "tsx prisma/seed.ts"
}
```
  </action>
  <verify>
    <automated>cd /c/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application && grep -c "export" packages/types/src/index.ts && grep "admin@msp.local" packages/db/prisma/seed.ts | head -1</automated>
  </verify>
  <acceptance_criteria>
    - packages/types/src/auth.ts contains "loginSchema", "jwtPayloadSchema", "apiKeyCreateSchema"
    - packages/types/src/tenant.ts contains "createTenantSchema", "tenantTypeSchema"
    - packages/types/src/common.ts contains "paginationSchema", "apiErrorSchema"
    - packages/types/src/index.ts re-exports from auth.ts, tenant.ts, common.ts
    - packages/db/prisma/seed.ts contains "admin@msp.local", "Agent123!", "User123!"
    - packages/db/prisma/seed.ts creates 4 system roles: admin, msp_admin, agent, end_user
    - packages/db/prisma/seed.ts creates 4 subscription plans: STARTER, PROFESSIONAL, BUSINESS, ENTERPRISE
    - packages/db/prisma/seed.ts creates default SLA with p1-p4 response and resolution times
    - packages/db/prisma/seed.ts creates 5 default categories
    - packages/db/package.json contains "prisma" section with "seed" script
  </acceptance_criteria>
  <done>Zod schemas for auth, tenant, and common inputs/outputs are exported. Seed script creates the default tenant, system roles, test users, SLA policies, categories, subscription plans, and a sample customer organization.</done>
</task>

</tasks>

<verification>
- `npx prisma generate` succeeds
- Schema contains 55+ model declarations
- Every tenant-scoped model has `tenantId` and `@@index([tenantId])`
- Global models (Tenant, OwnerUser, etc.) do NOT have tenantId
- Tenant extension in packages/db/src/extensions/tenant.ts exports `withTenantScope`
- Zod schemas compile and export from packages/types
- Seed script references correct test credentials
</verification>

<success_criteria>
The complete Prisma schema with all 61 models is defined, generates successfully, and includes tenant scoping infrastructure. The types package exports Zod validation schemas. The seed script is ready to populate the database with default data once migrations run against a live PostgreSQL instance.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
