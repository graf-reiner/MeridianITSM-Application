---
phase: 04-cmdb-change-management-and-asset-portfolio
plan: 07
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - apps/web/src/app/dashboard/assets/page.tsx
  - apps/web/src/app/dashboard/assets/[id]/page.tsx
  - apps/web/src/app/dashboard/cmdb/page.tsx
  - apps/web/src/app/dashboard/cmdb/[id]/page.tsx
  - apps/web/src/app/dashboard/changes/page.tsx
  - apps/web/src/app/dashboard/changes/new/page.tsx
  - apps/web/src/app/dashboard/changes/[id]/page.tsx
  - apps/web/src/app/dashboard/changes/calendar/page.tsx
  - apps/web/src/app/dashboard/cab/page.tsx
  - apps/web/src/app/dashboard/cab/[id]/page.tsx
autonomous: true
requirements: [ASST-01, ASST-02, ASST-03, ASST-05, CMDB-01, CMDB-03, CMDB-05, CMDB-06, CMDB-09, CHNG-01, CHNG-02, CHNG-03, CHNG-04, CHNG-05, CHNG-07, CHNG-08, CHNG-09, CAB-01, CAB-02, CAB-03, CAB-04, CAB-05]

must_haves:
  truths:
    - "Staff can view asset list with filters and asset detail with lifecycle and assignment info"
    - "Staff can view CI list and CI detail with relationship map, change history, and ticket links"
    - "Staff can create changes with type-dependent form fields (emergency hides scheduling)"
    - "Change detail page shows inline approval panel with approve/reject buttons"
    - "Change calendar shows month view with changes as color-coded bars by risk level"
    - "CAB meeting detail shows agenda, attendee RSVP, iCal download, and per-change voting buttons"
  artifacts:
    - path: "apps/web/src/app/dashboard/assets/page.tsx"
      provides: "Asset list page with status/site/search filters"
    - path: "apps/web/src/app/dashboard/assets/[id]/page.tsx"
      provides: "Asset detail with lifecycle badge and purchase tracking"
    - path: "apps/web/src/app/dashboard/cmdb/page.tsx"
      provides: "CI list page with type/status filters"
    - path: "apps/web/src/app/dashboard/cmdb/[id]/page.tsx"
      provides: "CI detail with relationship map, change history, impact analysis"
    - path: "apps/web/src/app/dashboard/changes/page.tsx"
      provides: "Change list with status/type filters"
    - path: "apps/web/src/app/dashboard/changes/[id]/page.tsx"
      provides: "Change detail with inline approval panel and audit trail"
    - path: "apps/web/src/app/dashboard/changes/calendar/page.tsx"
      provides: "Change calendar month view"
    - path: "apps/web/src/app/dashboard/cab/[id]/page.tsx"
      provides: "CAB meeting detail with agenda, RSVP, iCal, voting"
  key_links:
    - from: "apps/web/src/app/dashboard/cmdb/[id]/page.tsx"
      to: "/api/v1/cmdb/cis/:id/impact"
      via: "fetch for impact analysis"
      pattern: "api/v1/cmdb.*impact"
    - from: "apps/web/src/app/dashboard/changes/[id]/page.tsx"
      to: "/api/v1/changes/:id/approve"
      via: "fetch for inline approval"
      pattern: "api/v1/changes.*approve"
    - from: "apps/web/src/app/dashboard/cab/[id]/page.tsx"
      to: "/api/v1/cab/meetings/:id/ical"
      via: "download link for iCal"
      pattern: "api/v1/cab.*ical"
---

<objective>
Build all staff-facing dashboard pages for assets, CMDB, change management, and CAB meetings, including the CMDB relationship map with impact analysis overlay, change calendar, inline approval panel, and CAB meeting detail with iCal download.

Purpose: The frontend pages make the Phase 4 backend APIs usable by MSP technicians. The visual CMDB map, change calendar, and inline approval panel are the user-facing differentiators.
Output: 10 new dashboard pages covering all 4 sub-domains.
</objective>

<execution_context>
@C:/Users/greiner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/greiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cmdb-change-management-and-asset-portfolio/04-CONTEXT.md
@.planning/phases/04-cmdb-change-management-and-asset-portfolio/04-RESEARCH.md

<interfaces>
<!-- API endpoints from Plans 01, 02, 03 -->
Asset API:
  GET /api/v1/assets?status=&assignedToId=&siteId=&search=&page=&pageSize=
  GET /api/v1/assets/:id
  POST /api/v1/assets { serialNumber, manufacturer, model, ... }
  PUT /api/v1/assets/:id

CMDB API:
  GET /api/v1/cmdb/cis?type=&status=&environment=&search=&page=&pageSize=
  GET /api/v1/cmdb/cis/:id (includes relations, change history, ticket links)
  GET /api/v1/cmdb/cis/:id/impact?depth=2
  GET /api/v1/cmdb/cis/:id/relationships
  GET /api/v1/cmdb/cis/:id/history

Change API:
  GET /api/v1/changes?status=&type=&riskLevel=&search=&page=&pageSize=
  GET /api/v1/changes/:id
  POST /api/v1/changes { title, type, ... }
  POST /api/v1/changes/:id/transition { status }
  POST /api/v1/changes/:id/approve { decision, comments? }
  GET /api/v1/changes/calendar?start=&end=

CAB API:
  GET /api/v1/cab/meetings
  GET /api/v1/cab/meetings/:id
  POST /api/v1/cab/meetings/:id/rsvp { rsvpStatus }
  POST /api/v1/cab/meetings/:id/changes/:changeId/outcome { outcome, notes? }
  GET /api/v1/cab/meetings/:id/ical

Existing frontend patterns:
  apps/web/src/app/dashboard/tickets/page.tsx — list page with TanStack Query, filters, Tailwind
  apps/web/src/app/dashboard/settings/sla/page.tsx — CRUD admin page
  apps/web/src/app/dashboard/reports/page.tsx — Recharts integration
  Icon usage: import Icon from '@mdi/react'; import { mdiXxx } from '@mdi/js';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Asset and CMDB dashboard pages with ReactFlow relationship map</name>
  <files>apps/web/src/app/dashboard/assets/page.tsx, apps/web/src/app/dashboard/assets/[id]/page.tsx, apps/web/src/app/dashboard/cmdb/page.tsx, apps/web/src/app/dashboard/cmdb/[id]/page.tsx</files>
  <read_first>
    apps/web/src/app/dashboard/tickets/page.tsx
    apps/web/src/app/dashboard/layout.tsx
    apps/web/src/app/dashboard/knowledge/page.tsx
  </read_first>
  <action>
Install ReactFlow dependencies in web app:
  pnpm --filter web add @xyflow/react @dagrejs/dagre
  pnpm --filter web add -D @types/dagre

Create apps/web/src/app/dashboard/assets/page.tsx:
- 'use client'
- TanStack Query: useQuery for GET /api/v1/assets with filter state
- Filters bar: status dropdown (IN_STOCK, DEPLOYED, IN_REPAIR, RETIRED, DISPOSED), site dropdown, search input
- Table layout: columns for assetTag, manufacturer+model, status badge (color-coded: green=DEPLOYED, blue=IN_STOCK, yellow=IN_REPAIR, gray=RETIRED/DISPOSED), assignedTo, site, warrantyExpiry
- Pagination component
- "New Asset" button linking to a create modal or inline form
- Icons: mdiDesktopClassic for page header

Create apps/web/src/app/dashboard/assets/[id]/page.tsx:
- 'use client', useParams for id, useQuery for GET /api/v1/assets/:id
- Header: assetTag (bold) + status badge
- Two-column layout:
  Left: Asset details card (manufacturer, model, serialNumber, hostname, OS, CPU, RAM)
  Right: Assignment card (assignedTo user, site), Purchase card (purchaseDate, purchaseCost formatted as currency, warrantyExpiry with color: red if expired, yellow if < 30 days, green otherwise)
- Status lifecycle visualization: horizontal bar showing all 5 statuses with current one highlighted
- Edit button (opens inline edit form or modal)

Create apps/web/src/app/dashboard/cmdb/page.tsx:
- 'use client'
- TanStack Query: useQuery for GET /api/v1/cmdb/cis
- Filters: type dropdown (9 CI types), status dropdown (4 statuses), environment dropdown, search input
- Table: ciNumber (CI-{number}), name, type icon (mdiServer for SERVER, mdiDesktopClassic for WORKSTATION, mdiLanConnect for NETWORK_DEVICE, mdiDatabase for DATABASE, etc.), status badge, environment badge, createdAt
- Pagination
- Buttons: "New CI", "Import CIs" (links to /dashboard/cmdb/import)

Create apps/web/src/app/dashboard/cmdb/[id]/page.tsx:
- 'use client'
- useQuery for GET /api/v1/cmdb/cis/:id (includes relations, history, ticket links)
- Header: CI name + ciNumber + type icon + status badge
- Tabs or sections:
  1. Details: attributesJson fields rendered as key-value pairs, environment, category, owner, site, linked asset
  2. Relationship Map (CMDB-09): ReactFlow component
     - 'use client' sub-component (or dynamic import with ssr: false)
     - Import { ReactFlow, Background, Controls, useNodesState, useEdgesState } from '@xyflow/react'
     - Import '@xyflow/react/dist/style.css'
     - Import dagre from '@dagrejs/dagre'
     - Build nodes from CI + its source/target relations
     - Custom node component: shows CI type icon with colored border (green=ACTIVE, yellow=MAINTENANCE, gray=INACTIVE per CONTEXT.md)
     - Apply dagre layout with rankdir: 'TB' (top-down), ranksep: 80, nodesep: 40
     - "Impact Analysis" button: fetches GET /api/v1/cmdb/cis/:id/impact?depth=2
       When active: affected CIs get red/orange border + glow effect (data.impacted=true), unaffected ones dim to opacity 0.3
       Per CONTEXT.md: "colored overlay on the same map"
     - "Expand" on any edge node to load more (increase depth by 1, re-fetch)
     - Default traversal depth: 2 levels per CONTEXT.md
  3. Change History: timeline of CmdbChangeRecord entries (fieldName, oldValue -> newValue, changedBy, timestamp)
  4. Linked Tickets: list of tickets linked via CmdbTicketLink with ticket number + title
  </action>
  <verify>
    <automated>cd C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application && ls apps/web/src/app/dashboard/assets/page.tsx apps/web/src/app/dashboard/assets/*/page.tsx apps/web/src/app/dashboard/cmdb/page.tsx apps/web/src/app/dashboard/cmdb/*/page.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/web/src/app/dashboard/assets/page.tsx (asset list page exists)
    - test -f apps/web/src/app/dashboard/assets/[id]/page.tsx (asset detail exists)
    - test -f apps/web/src/app/dashboard/cmdb/page.tsx (CI list page exists)
    - test -f apps/web/src/app/dashboard/cmdb/[id]/page.tsx (CI detail exists)
    - grep -q "@xyflow/react" apps/web/src/app/dashboard/cmdb/[id]/page.tsx (ReactFlow used)
    - grep -q "dagre" apps/web/src/app/dashboard/cmdb/[id]/page.tsx (dagre layout)
    - grep -q "impact" apps/web/src/app/dashboard/cmdb/[id]/page.tsx (impact analysis button/fetch)
    - grep -q "ASSET_TRANSITIONS\|IN_STOCK\|DEPLOYED" apps/web/src/app/dashboard/assets/[id]/page.tsx || grep -q "status" apps/web/src/app/dashboard/assets/[id]/page.tsx (status lifecycle shown)
    - grep -q "useQuery" apps/web/src/app/dashboard/assets/page.tsx (TanStack Query)
    - grep -q "useQuery" apps/web/src/app/dashboard/cmdb/page.tsx (TanStack Query)
  </acceptance_criteria>
  <done>Asset list and detail pages show all asset data with status lifecycle visualization, purchase tracking, and assignment info. CMDB list and detail pages show CI data with ReactFlow relationship map (dagre top-down layout, CI type icons, status-colored borders), impact analysis overlay (red/orange affected CIs, dimmed unaffected), change history timeline, and linked tickets.</done>
</task>

<task type="auto">
  <name>Task 2: Change management and CAB dashboard pages</name>
  <files>apps/web/src/app/dashboard/changes/page.tsx, apps/web/src/app/dashboard/changes/new/page.tsx, apps/web/src/app/dashboard/changes/[id]/page.tsx, apps/web/src/app/dashboard/changes/calendar/page.tsx, apps/web/src/app/dashboard/cab/page.tsx, apps/web/src/app/dashboard/cab/[id]/page.tsx</files>
  <read_first>
    apps/web/src/app/dashboard/tickets/page.tsx
    apps/web/src/app/dashboard/reports/page.tsx
  </read_first>
  <action>
Create apps/web/src/app/dashboard/changes/page.tsx:
- 'use client'
- TanStack Query for GET /api/v1/changes
- Filters: status dropdown (10 change statuses), type dropdown (STANDARD/NORMAL/EMERGENCY), riskLevel dropdown, search
- Table: changeNumber (CHG-{number}), title, type badge (EMERGENCY gets red "EMERGENCY" badge per CONTEXT.md), status badge, riskLevel badge (color by risk: green=LOW, yellow=MEDIUM, red=HIGH, dark-red=CRITICAL), requestedBy, scheduledStart
- "New Change" button linking to /dashboard/changes/new
- "Calendar View" toggle/button linking to /dashboard/changes/calendar
- Pagination

Create apps/web/src/app/dashboard/changes/new/page.tsx:
- 'use client'
- React Hook Form + Zod validation
- Type selector at top (STANDARD/NORMAL/EMERGENCY) — changes form fields below per CONTEXT.md:
  - Always shown: title, description, riskLevel
  - Hidden for EMERGENCY: scheduledStart, scheduledEnd, implementationPlan (per CONTEXT.md: "no scheduling dates, no implementation plan required")
  - Hidden for STANDARD: approval section (auto-approved per CONTEXT.md: "skip approval chain entirely")
  - NORMAL: show all fields including implementationPlan, backoutPlan, testingPlan, scheduledStart/End, approvers multi-select
- Submit: POST /api/v1/changes
- On success: redirect to change detail page

Create apps/web/src/app/dashboard/changes/[id]/page.tsx:
- 'use client', useParams for id
- useQuery for GET /api/v1/changes/:id
- Header: changeNumber + title + type badge + status badge
- EMERGENCY changes: red "EMERGENCY" badge everywhere per CONTEXT.md
- Inline approval panel at top (per CONTEXT.md: "approval panel at top showing pending action"):
  - If current user is the next approver (lowest sequenceOrder with status=PENDING):
    Show "Approve" and "Reject" buttons. Reject requires comment input.
    POST /api/v1/changes/:id/approve with { decision, comments? }
  - Show approval chain: list of approvers with status icons (green check=APPROVED, red X=REJECTED, gray clock=PENDING)
- Sections:
  1. Details: description, implementationPlan, backoutPlan, testingPlan (collapsible)
  2. Schedule: scheduledStart/End with collision warning (GET /api/v1/changes/:id/collisions)
  3. Linked Assets: list with "Link Asset" button
  4. Linked Applications: list with "Link Application" button
  5. Activity Trail: ChangeActivity entries in timeline format
- Status transition buttons: based on current status, show allowed next statuses as action buttons (POST /api/v1/changes/:id/transition)

Create apps/web/src/app/dashboard/changes/calendar/page.tsx:
- 'use client'
- Month calendar view per CONTEXT.md: "month view with day cells showing change bars spanning scheduled windows"
- Fetch: GET /api/v1/changes/calendar?start={monthStart}&end={monthEnd}
- Build a CSS grid calendar (7 cols for days, 5-6 rows for weeks)
- Each change with scheduledStart/End renders as a horizontal bar spanning the appropriate days
- Color by risk level per CONTEXT.md: green=LOW, yellow=MEDIUM, red=HIGH
- Click a change bar to navigate to /dashboard/changes/:id
- Month navigation: previous/next buttons, current month heading

Create apps/web/src/app/dashboard/cab/page.tsx:
- 'use client'
- TanStack Query for GET /api/v1/cab/meetings
- Table: title, scheduledFor (formatted date/time), status badge, attendee count, change count
- "New CAB Meeting" button (opens create form/modal)

Create apps/web/src/app/dashboard/cab/[id]/page.tsx:
- 'use client', useParams for id
- useQuery for GET /api/v1/cab/meetings/:id
- Header: meeting title + scheduledFor + status badge + "Download iCal" button (href to /api/v1/cab/meetings/:id/ical, triggers download)
- Meeting info: location, meetingUrl (clickable link), durationMinutes, notes
- Attendees section per CONTEXT.md: "attendee list with RSVP status"
  - Table of attendees: user name, role badge (CHAIRPERSON/MEMBER/OBSERVER), RSVP status badge (green=ACCEPTED, yellow=TENTATIVE, red=DECLINED, gray=PENDING)
  - If current user is an attendee: RSVP buttons (Accept, Decline, Tentative) — POST /api/v1/cab/meetings/:id/rsvp
  - "Add Attendee" button for meeting organizers
- Agenda section per CONTEXT.md: "linked changes in display order" with "approve/reject/defer voting buttons"
  - Ordered list of linked changes (by agendaOrder)
  - Each change shows: changeNumber, title, type badge, current status
  - Per-change voting buttons: APPROVED, REJECTED, DEFERRED, NEEDS_MORE_INFO — POST /api/v1/cab/meetings/:id/changes/:changeId/outcome
  - Show existing outcome if already voted
  </action>
  <verify>
    <automated>cd C:/Users/greiner/OneDrive/ClaudeAI/MeridianITSM-Application && ls apps/web/src/app/dashboard/changes/page.tsx apps/web/src/app/dashboard/changes/new/page.tsx apps/web/src/app/dashboard/changes/*/page.tsx apps/web/src/app/dashboard/cab/page.tsx apps/web/src/app/dashboard/cab/*/page.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/web/src/app/dashboard/changes/page.tsx (change list exists)
    - test -f apps/web/src/app/dashboard/changes/new/page.tsx (change create form exists)
    - test -f apps/web/src/app/dashboard/changes/[id]/page.tsx (change detail exists)
    - test -f apps/web/src/app/dashboard/changes/calendar/page.tsx (calendar view exists)
    - test -f apps/web/src/app/dashboard/cab/page.tsx (CAB list exists)
    - test -f apps/web/src/app/dashboard/cab/[id]/page.tsx (CAB detail exists)
    - grep -q "EMERGENCY" apps/web/src/app/dashboard/changes/new/page.tsx (type-dependent form)
    - grep -q "approve\|Approve" apps/web/src/app/dashboard/changes/[id]/page.tsx (inline approval)
    - grep -q "calendar\|Calendar" apps/web/src/app/dashboard/changes/calendar/page.tsx
    - grep -q "ical" apps/web/src/app/dashboard/cab/[id]/page.tsx (iCal download)
    - grep -q "rsvp\|RSVP" apps/web/src/app/dashboard/cab/[id]/page.tsx (RSVP buttons)
    - grep -q "outcome\|APPROVED\|REJECTED\|DEFERRED" apps/web/src/app/dashboard/cab/[id]/page.tsx (voting buttons)
  </acceptance_criteria>
  <done>Change management pages include: list with filters, type-dependent create form (emergency hides scheduling), detail with inline approval panel and status transition buttons, and month calendar view with risk-colored change bars. CAB pages include: meeting list and detail with agenda ordering, RSVP buttons, iCal download, and per-change voting buttons.</done>
</task>

</tasks>

<verification>
- All 10 page files exist in dashboard directory
- ReactFlow used in CMDB detail: grep "@xyflow/react" apps/web/src/app/dashboard/cmdb/[id]/page.tsx
- Impact analysis wired: grep "impact" apps/web/src/app/dashboard/cmdb/[id]/page.tsx
- Inline approval: grep "approve" apps/web/src/app/dashboard/changes/[id]/page.tsx
- Calendar view: test -f apps/web/src/app/dashboard/changes/calendar/page.tsx
- iCal download: grep "ical" apps/web/src/app/dashboard/cab/[id]/page.tsx
</verification>

<success_criteria>
- Asset list with status/site/search filters and detail with lifecycle visualization, purchase tracking
- CI list with type/status/environment filters and detail with ReactFlow relationship map (dagre top-down layout)
- CI detail has impact analysis overlay: affected CIs glow red/orange, unaffected dim (CONTEXT.md locked decision)
- CI nodes show type icon with status-colored border (green=active, yellow=maintenance, gray=inactive)
- Default traversal depth 2 with "expand" on edge nodes
- Change list with type-dependent badges, create form with type-dependent field visibility
- Change detail has inline approval panel at top (CONTEXT.md locked decision: no separate approval page)
- Change calendar: month view with risk-colored bars (green=low, yellow=medium, red=high)
- Emergency changes have red "EMERGENCY" badge everywhere
- CAB meeting detail: agenda with display order, attendee RSVP, iCal download button, per-change voting
</success_criteria>

<output>
After completion, create `.planning/phases/04-cmdb-change-management-and-asset-portfolio/04-07-SUMMARY.md`
</output>
