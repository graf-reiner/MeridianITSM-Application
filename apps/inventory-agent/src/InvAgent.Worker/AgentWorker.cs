namespace InvAgent.Worker;

/// <summary>
/// Background worker that runs heartbeat and inventory collection loops.
/// Full implementation in Plan 05-05 (worker + HTTP + queue).
/// </summary>
public class AgentWorker
{
    // Stub — implementation in next plan
}
