namespace InvAgent.Queue;

/// <summary>
/// Local SQLite-backed queue for offline buffering of inventory payloads.
/// Full implementation in Plan 05-05 (worker + HTTP + queue).
/// </summary>
public class LocalQueue
{
    // Stub — implementation in next plan
}
